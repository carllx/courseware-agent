# 导师提词器 (Teacher Guide) UX 体验审查与重构前瞻报告

> **目标读者**：UX / 交互设计师、教研产品经理
> **报告目的**：梳理目前 H5 `?mode=teacher` （教师模式）测试版上线后收到的焦点负面反馈。追溯技术归因，将当前的“黑盒逻辑”与“交互痛点”白盒化，并为下一阶段的重构提供基础决策材料。

---

## 1. 当前需求背景与架构原委 (Context)

在新版的教学 H5 引擎中，我们引入了基于 **Rosenshine 认知规律**的“温度分类（Cold/Hot）”机制，替代了旧版的 SCQA 进度条。
- **痛点初衷**：在实际授课现场，教师使用移动端（iPad/手机）进行投屏讲课时，缺乏光标悬停（Hover）能力，导致原定隐藏在标签中的备课提示无法唤出。
- **当前解法**：开发了一套底层的 `TeacherGuideSheet`（底部抽屉组件）。当进入 `?mode=teacher` 时，劫持底部导航胶囊的点击事件，改为向上弹出一张包含备课建议的情绪提示卡。

---

## 2. 当前遇到的三大用户体验阻碍与技术归因

### 🐛 痛点 1：导航被劫持，违背肌肉记忆
* **用户反馈**：“下面的 dot 我点击不能切换 slider”*
* **技术现状**：当前在 `LessonViewer.jsx` 中，只要处于教师模式，点击进度条的胶囊将**被强制拦截**并弹起 Bottom Sheet，而彻底封堵了其原有的“切换幻灯片”职能。
* **UX 冲突**：进度条/滑块在用户的默认认知模型中 100% 用于“页面跳转”。将“看提示”与“翻页”绑定在同一个物理触点上，造成了严重的认知失调。

### 🐛 痛点 2：模式状态丢失，缺乏常驻感知
* **用户反馈**：“点击导播后又退出了教师模式，却又回不来”*
* **技术现状**：当教师在弹出的卡片里点击“同步大图展示此页”时，系统调用了 `switchSlide()` 并关闭了弹窗。虽然此时 URL 依然带有 `?mode=teacher`，系统也确切处于教师模式，但 UI 上**没有任何常驻视觉标识**。
* **UX 冲突**：弹窗消失后，界面看起来和“学生版”一模一样。用户产生了“我被系统踢出教师模式”的恐慌感。

### 🐛 痛点 3：分类黑盒，导致对“准确性”的质疑
* **用户反馈**：“不知道当前 sheet-title 的类型判别标准是什么？是否准确？”*
* **技术现状 (黑盒揭秘)**：
  目前后端的 Python 解析器 (`generate_course_h5.py`) 根据 markdown 中的标签进行粗粒度分类：
  - **🔥 Hot (情绪池)**：包含 `STORY TIME`, `CASE STUDY`, `LIFE CONNECT`, `PHILOSOPHY`, `DID YOU KNOW` 的段落。
  - **🧊 Cold (硬核区)**：包含 `TECH NOTE`, `TEACHING MOMENT`, `WARNING` 的段落。
  - 前端计算该 Slide 中所有字符，如果 Hot 字符数 >= Cold 字符数，则判定该页为 `temp-hot`，直接暴力输出“高点情绪池”。
* **UX 冲突**：教师备课时，看到“高点情绪池”这种定性结论，却看不到支撑该结论的证据（比如：本页到底有个什么 Case Study？）。这种“AI 爹味指导”削弱了工具的可靠性。

---

## 3. 为设计团队提供的四大破局提案 (Optimization Proposals)

请设计团队审阅以上机制后，围绕以下四个切入点产出新版 UI/UX 原型：

### 💡 提案 A：解绑“翻页”与“看提示”（触点分离）
**必须将导航条的“翻页”权利还给用户。**
- 原有的底部 Timeline 胶囊无论在什么模式下，点击都立即跳转幻灯片。
- **新增专注触点**：引入一个常驻的浮动按钮（FAB，悬浮在右下角，如一个 🎒 `备课囊` 图标）或在顶栏增加一个 `💡 讲师锦囊` 按钮。点击它时，弹出**针对当前页面**的 Bottom Sheet。

### 💡 提案 B：建立明确的“教师模式”结界 (Persistent Context)
在检测到 `mode=teacher` 时，为整个 H5 增添全局性的视觉环境：
- 添加一圈微弱的品牌色边框。
- 顶部导航栏增加一条常驻提醒：`👁️ 教师实况导播模式中`。
- 只有这样，模态框关闭后用户才不会觉得遭遇了“模式断联”。

### 💡 提案 C：从“AI 总结”转向“证据白盒”
不要只给“高点情绪池”的空洞大标题。要在 Bottom Sheet 中加入数据拆解透视板：
- *修改前*：“当前段落流属性：HOT”
- *修改后*：
  > **核心备课组件提取**：
  > 📦 包含 1 个 **CASE STUDY** (丰田考斯特案例，约 210 字)
  > 📦 包含 1 个 **WARNING** (数据偏差防坑，约 80 字)

### 💡 提案 D：重塑“远程导播”VS“备课”的场景定义
目前卡片上有个按钮叫“导播：同步大图”。设计团队需要明确界定，这个界面的最终场景是：
1. **讲台前手持**：教师在手机上看，大屏幕在背后同步翻页。
2. **睡前备课**：教师在 iPad 上自我推演。
如果是前者，目前的手机端/大屏端的 WebSocket 同步能力尚未建立；如果是后者，“导播”按钮就是个伪需求。需要重新敲定卡片底部的核心 CTA 按钮策略。
