import sys

with open("M04_Vibe_Coding_排错工作流.md", "r", encoding="utf-8") as f:
    content = f.read()

old_text = """如果没有任何报错红字，但坐标系依然消失了呢？这说明第一层漏斗未能拦截问题，此时我们必须升格为结构化定位指令：
`1. 目标预期：应该在画布底部展现 X 轴。`
`2. 实际行为：画布为空，审查元素发现 <g>（SVG的编组容器，类似 PS 中的图层组）的 translate 坐标（平移属性）出现了负数，完全越界飞出了屏幕。`
`3. 现场代码提取：附上 margin（边缘留白）的定义和生成 X 轴的 5 行核心代码块`

> [VISUAL]
> *   **Slide**: S16b_Structural_Spotlight
> *   **Asset**: ![预览](../public/slides/s16b_structural_spotlight_1779786101697.png)
> *   **Layout**: Center
> *   **Scene**: 在代码迷宫中，一道聚光灯锁定在特定 5 行核心代码上。
> *   **Text**: 结构化定位：打探照灯

这样写的好处是，如同给 AI 戴上了探照灯，限制它只能在你锁定的这 5 行代码里寻找逻辑错误。

### 4.4 终极制裁：最小可复现示例能彻底隔离代码病灶

> [VISUAL]
> *   **Slide**: S17_MRE_Isolation
> *   **Asset**: ![预览](../public/slides/s17_mre_isolation_1773994221094.png)
> *   **Layout**: Split
> *   **Scene**: 左侧是密密麻麻、布满几千行代码和复杂样式的死档屏；右侧是经过剥离后，只剩下五行核心代码和一个清爽运行环境的纯净界面。
> *   **Text**: 隔离病灶，才能施展精准打击

当前两层漏斗失效时，你需要动用终极拆解策略。

> [STORY TIME] 为什么你的求救没人理？
> 在许多开发者论坛里，每天都有人贴出长达三千多行的代码文件，哭诉“我的图表挂了，为什么？”然而，这种求救帖往往几个月都不会有人回复。
> 为什么？因为噪音太大。没有任何人或 AI 愿意在几千行乱成一锅粥的代码里找错。**这就像你发给别人一个包含 800 个未命名图层的 AE 工程文件，让人帮你找为什么画面少了一帧。**
> 只有当你把无关的特效全关掉，只保留出问题的那个图层——在编程里，把代码简化到不能再简化的纯净状态，这叫做 **MRE（最小可复现示例）**——这时候，AI 的算力才不会被干扰，瞬间就能帮你锁定病灶。

> [VISUAL]
> *   **Slide**: S17b_Surgical_MRE
> *   **Asset**: ![预览](../public/slides/s17b_surgical_mre_1779786121237.png)
> *   **Layout**: Center
> *   **Scene**: 抽象的代码结构图，展示复杂代码被强行分离，仅剩下核心数据部分。
> *   **Text**: 排错杀手锏：断腕隔离

如果前两招失效，你需要强行剥离无关代码。**但作为初学者，千万不要在几千行报错代码中手工删减，那极易误删关键括号导致二次崩溃。**
你需要做的是**命令 AI 降维或开启新对话**，要求它退回最纯净的环境：“暂时忽略网格、坐标轴和悬浮框，给我一个只有 3 条数据、最基础的 D3 散点图，先让纯粹的圆画在画布上。”

当 AI 处理的代码量级从数百行骤降到寥寥几十字，它的逻辑推导能力会大幅上升。一旦基础 MRE 跑通，再让 AI 逐步把动效和数据加回去。这就是 MRE 控制法的核心作用。通过建立这三重防线，你不仅能修复图表项目，更能反向培养结构化的工程直觉。

> [VISUAL]
> *   **Slide**: S17c_Navigating_Minefield
> *   **Asset**: ![预览](../public/slides/s17c_navigating_minefield_1779786138525.png)
> *   **Layout**: Center
> *   **Scene**: 在布满代码碎片的雷区中，通过层层过滤器的防护网稳步前行的排错者身影。
> *   **Text**: 穿越模型幻觉雷区

好，为了检验大家是否真正掌握了这一反本能的排错直觉，我们来做一个情境测验：

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `3min`
> *   **Desc**: 随堂测验：MRE 隔离法则
> *   **Q**: 小林正在用大模型生成带有复杂悬浮动效和 500 条数据的 D3 气泡图，但页面完全白屏且无报错红字。根据 MRE（最小可复现示例）法则，她此时最合理的下一步做法是什么？
> *   **Options**: A. 开启新对话或发送降维指令，要求 AI 暂时忽略动效与网格，先输出仅含 3 条数据的最基础纯净代码跑通渲染 | B. 保持现有工程结构的完整性，将这几百行原始代码全部复制给 AI，并附言“渲染失败，请整体审查” | C. 放弃已有的代码，直接用自然语言向大模型描述“画布完全白屏，怀疑是坐标轴出错”，要求其重新生成 | D. 在浏览器开发者工具中寻找警告信息，结合猜测将其翻译成中文交给大模型，请求它提供具体的解决方案
> *   **Answer**: A
> *   **Explain**: 正确答案是 A。当控制台无红字（第一层漏斗失效）且无法定位特定结构（第二层漏斗失效）时，必须动用终极拆解策略。选项 A 符合本节讲授的 MRE 原则——“把代码简化到不能再简化的纯净状态”，同时通过向 AI 发送降维指令，避免了初学者手工删改导致的二次崩溃。选项 B 会制造巨大的噪音死档；选项 C 虽然也是重新生成，但没有隔离出最小纯净状态（MRE）；选项 D 违反了第一层漏斗中“原样抄写、不要自己翻译”的心法。参见本节关于“终极制裁 MRE”的论述。

> [ACTIVITY]
> *   **Type**: `Workshop`
> *   **Duration**: `45min`
> *   **Desc**: **D3 幻觉抢修演练**。
>   (1) 讲师通过局域网，向所有机器分发一份含有隐蔽错误的代码（如没有设定 SVG 容器边界、`scale` 域（数据映射的比例尺范围）和数据类型强判定为 string（纯文本字符串，导致数字相加变成拼贴）等）。
>   (2) 全班按小组对抗竞争，禁止贴入整个文件询问。必须动用漏斗三连击——查红字、结构定位、MRE 阻断剥离法。通过最少 Prompt 次数让 AI 给出精准修正代码的组获胜。
>   (3) 复盘：胜利者分享剥离无关代码、快速锁定错误的过程。

---

## 课后作业与综合演练
<!-- BUDGET: 0 chars | TYPE: activity | STATUS: exempt -->

> [VISUAL]
> *   **Slide**: S18_Final_Lab
> *   **Asset**: ![预览](../public/slides/s18_final_lab_1779786154788.png)
> *   **Layout**: Split
> *   **Scene**: 带有警报色和倒计时的数字作战室全景，左右全息大屏分别展示着商业级面板与前卫生成艺术图。
> *   **Text**: 大课挑战：双轨对决
> *   **List**: 商业实战, 前卫艺术

> [ACTIVITY]
> *   **Type**: `Practice`
> *   **Duration**: `60min`
> *   **Desc**: **大课作业 (Lab Session)：双轨范式演练**
>   采用上周清洗出的 Tidy 数据集进行实操，提交 A+B 独立全包 HTML：
>   - **作品 A (ECharts 商业版)**：生成带 Zoom、Tooltip 与图例筛选的多轴组件。
>   - **作品 B (D3 前卫版)**：实现异形空间映射（如雨点下坠图、蜂巢散点地图等），必须附上与 AI 的排错交锋实录。

今天的课就到这里。大模型让生产工具前所未有地唾手可得。
但请记住，工具使用手册已经不值钱。能看懂数据骨架、掌控空间分配，并拥有漏斗排错思维的设计师，永远不可替代。"""

new_text = """如果没有任何报错红字，但坐标系依然消失了呢？这说明第一层漏斗未能拦截问题，此时我们必须升格为**结构化定位指令**：
`1. 目标预期：应该在画布底部展现 X 轴。`
`2. 实际行为：画布为空，审查元素发现 <g> 标签（SVG的编组容器，类似 PS 中的图层组）的 translate 坐标（平移属性）出现了负数，完全越界飞出了屏幕。`
`3. 现场代码提取：附上 margin（边缘留白）的定义和生成 X 轴的 5 行核心代码块`

> [VISUAL]
> *   **Slide**: S16b_Structural_Spotlight
> *   **Asset**: ![预览](../public/slides/s16b_structural_spotlight_1779786101697.png)
> *   **Layout**: Center
> *   **Scene**: 在代码迷宫中，一道聚光灯锁定在特定 5 行核心代码上。
> *   **Text**: 结构化定位：打探照灯

这样写的好处是，如同给 AI 戴上了**探照灯**，限制它**只能在你锁定的局部代码里**寻找逻辑错误。

### 4.4 终极制裁：最小可复现示例能彻底隔离代码病灶

> [VISUAL]
> *   **Slide**: S17_MRE_Isolation
> *   **Asset**: ![预览](../public/slides/s17_mre_isolation_1773994221094.png)
> *   **Layout**: Split
> *   **Scene**: 左侧是密密麻麻、布满几千行代码和复杂样式的死档屏；右侧是经过剥离后，只剩下五行核心代码和一个清爽运行环境的纯净界面。
> *   **Text**: 隔离病灶，才能施展精准打击

**但如果连审查元素都找不到具体的标签，整个文档彻底混乱了怎么办？** 当第一层红字拦截和第二层结构定位双双失效时，你就需要动用**终极拆解策略**。

> [STORY TIME] 为什么你的求救没人理？
> 在许多开发者论坛里，每天都有人贴出长达三千多行的代码文件，哭诉“我的图表挂了，为什么？”然而，这种求救帖往往几个月都不会有人回复。
> 为什么？因为噪音太大。没有任何人或 AI 愿意在几千行乱成一锅粥的代码里找错。**这就像你发给别人一个包含 800 个未命名图层的 AE 工程文件，让人帮你找为什么画面少了一帧。**
> 只有当你把无关的特效全关掉，只保留出问题的那个图层——在编程里，把代码简化到不能再简化的纯净状态，这叫做 **MRE（最小可复现示例）**——这时候，AI 的算力才不会被干扰，瞬间就能帮你锁定病灶。

> [VISUAL]
> *   **Slide**: S17b_Surgical_MRE
> *   **Asset**: ![预览](../public/slides/s17b_surgical_mre_1779786121237.png)
> *   **Layout**: Center
> *   **Scene**: 抽象的代码结构图，展示复杂代码被强行分离，仅剩下核心数据部分。
> *   **Text**: 排错杀手锏：断腕隔离

如果前两招失效，你需要**强行剥离无关代码**。但作为初学者，千万不要在几千行报错代码中手工删减，那极易误删关键括号导致二次崩溃。
你需要做的是**命令 AI 降维或开启新对话**，要求它退回最纯净的环境：“暂时忽略网格、坐标轴和悬浮框，给我一个只有 3 条数据、最基础的 D3 散点图，先让纯粹的圆画在画布上。”

当 AI 处理的代码量级从数百行骤降到寥寥几十字，它的**逻辑推导能力会大幅上升**。一旦基础 MRE 跑通，再让 AI 逐步把动效和数据加回去。通过建立这三重防线，你不仅能修复图表项目，更能反向培养**结构化的工程直觉**。

> [TEACHING MOMENT] 🧊 金句
> **排错的本质不是修补，而是控制变量。代码越少，AI 的智商就越高。**

> [VISUAL]
> *   **Slide**: S17c_Navigating_Minefield
> *   **Asset**: ![预览](../public/slides/s17c_navigating_minefield_1779786138525.png)
> *   **Layout**: Center
> *   **Scene**: 在布满代码碎片的雷区中，通过层层过滤器的防护网稳步前行的排错者身影。
> *   **Text**: 穿越模型幻觉雷区

好，为了检验大家是否真正掌握了这一反本能的排错直觉，我们来做一个情境测验：

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `3min`
> *   **Desc**: 随堂测验：MRE 隔离法则
> *   **Q**: 小林正在用大模型生成带有复杂悬浮动效和 500 条数据的 D3 气泡图，但页面完全白屏且无报错红字，**而且在审查元素时也找不到任何 SVG 结构标签**。根据 MRE（最小可复现示例）法则，她此时最合理的下一步做法是什么？
> *   **Options**: A. 开启新对话或发送降维指令，要求 AI 暂时忽略动效与网格，先输出仅含 3 条数据的最基础纯净代码跑通渲染 | B. 保持现有工程结构的完整性，将这几百行原始代码全部复制给 AI，并附言“渲染失败，请整体审查” | C. 放弃已有的代码，直接用自然语言向大模型描述“画布完全白屏，怀疑是坐标轴出错”，要求其重新生成 | D. 在浏览器开发者工具中寻找警告信息，结合猜测将其翻译成中文交给大模型，请求它提供具体的解决方案
> *   **Answer**: A
> *   **Explain**: 正确答案是 A。当控制台无红字（第一层漏斗失效）且**无法定位特定结构（第二层漏斗失效）**时，必须动用终极拆解策略。选项 A 符合本节讲授的 MRE 原则——“把代码简化到不能再简化的纯净状态”，同时通过向 AI 发送降维指令，避免了初学者手工删改导致的二次崩溃。选项 B 会制造巨大的噪音死档；选项 C 虽然也是重新生成，但没有隔离出最小纯净状态（MRE）；选项 D 违反了第一层漏斗中“原样抄写、不要自己翻译”的心法。

> [ACTIVITY]
> *   **Type**: `Workshop`
> *   **Duration**: `45min`
> *   **Desc**: **D3 幻觉抢修演练**。
>   (1) 讲师通过局域网，向所有机器分发一份含有隐蔽错误的代码（如没有设定 **SVG 容器边界**、`scale` 域和数据类型强判定错误等）。
>   (2) 全班按小组对抗竞争，禁止贴入整个文件询问。必须动用漏斗三连击——**查红字、结构定位、MRE 阻断剥离法**。通过最少 Prompt 次数让 AI 给出精准修正代码的组获胜。
>   (3) 复盘：胜利者分享**剥离无关代码、快速锁定错误**的过程。

---

## 课后作业与综合演练
<!-- BUDGET: 0 chars | TYPE: activity | STATUS: exempt -->

> [VISUAL]
> *   **Slide**: S18_Final_Lab
> *   **Asset**: ![预览](../public/slides/s18_final_lab_1779786154788.png)
> *   **Layout**: Split
> *   **Scene**: 带有警报色和倒计时的数字作战室全景，左右全息大屏分别展示着商业级面板与前卫生成艺术图。
> *   **Text**: 大课挑战：双轨对决
> *   **List**: 商业实战, 前卫艺术

> [ACTIVITY]
> *   **Type**: `Practice`
> *   **Duration**: `60min`
> *   **Desc**: **大课作业 (Lab Session)：双轨范式演练**
>   采用上周清洗出的 **Tidy 数据集**进行实操，提交 A+B **独立全包 HTML**：
>   - **作品 A (ECharts 商业版)**：生成带 Zoom、Tooltip 与图例筛选的**多轴组件**。
>   - **作品 B (D3 前卫版)**：实现**异形空间映射**（如雨点下坠图、蜂巢散点地图等），必须附上与 AI 的**排错交锋实录**。

今天的课就到这里。大模型让生产工具前所未有地唾手可得。
但请记住，工具使用手册已经不值钱。**能看懂数据骨架、掌控空间分配，并拥有漏斗排错思维的设计师，永远不可替代。**"""

if old_text in content:
    content = content.replace(old_text, new_text)
    with open("M04_Vibe_Coding_排错工作流.md", "w", encoding="utf-8") as f:
        f.write(content)
    print("Patch applied successfully.")
else:
    print("Error: Target content not found in file.")
    sys.exit(1)
