import sys

file_path = "/Users/yamlam/Downloads/2025-2026-2 课程/信息可视化/weeks/W04_AI_D3_Basics/src/M03_像素的绝对支配_D3.js.md"

with open(file_path, "r", encoding="utf-8") as f:
    content = f.read()

# Patch 1
t1 = """D3 由《纽约时报》前图形编辑 Mike Bostock 创建并开源。它不提供任何现成的图表，而是一种彻底的**指令式 (Imperative)** 工具。打个比方，使用 ECharts 就像去餐厅点一份“鱼香肉丝”（声明式，只看结果），而在 D3 的指令式世界里，你必须亲手控制刀工、火候、甚至每一粒盐的绝对落点。"""
r1 = """D3 不提供任何现成的图表，而是一种彻底的**指令式编程 (Imperative Programming)** 工具（即“把每一步怎么做的控制权完全抓在自己手里”）。打个比方，使用 ECharts 就像去餐厅点一份“鱼香肉丝”（声明式，只看结果），而在 D3 的指令式世界里，你必须亲手控制刀工、火候、甚至每一粒盐的绝对落点。"""

# Patch 2
t2 = """### 1.1 空间倒置：浏览器坐标系法则完全违背直觉

#### 1.1.1 颠覆常识：向下坠落的坐标系

> [VISUAL]
> *   **Slide**: S10b_Inverted_World
> *   **Asset**: ![预览](../public/slides/s10b_inverted_world_1779785545514.png)
> *   **Layout**: `Center`
> *   **Scene**: 以颠倒重力法则的视角俯视大地，建筑像钟乳石一样从天空向着地面下发疯狂生长。
> *   **Text**: 违背直觉的倒置画布

对于浏览器的发光机制来说，`(0,0)` 坐标原点默认定死在屏幕的**左上角**，Y 轴是**向下递增**的。这与人类自幼习惯的**左下角原点**的笛卡尔坐标系完全相反。也就是说，坐标数值从 0 变为 50，不是向上飞，而是意味着在屏幕上**向下坠落 50 像素**。这就是违背直觉的**倒置宇宙**。

> [VISUAL]
> *   **Slide**: S11_Inverted_Universe
> *   **Asset**: ![预览](../public/slides/s11_inverted_universe_1773994055292.png)
> *   **Layout**: `Split`
> *   **Scene**: 左侧是人类习惯的左下角原点向上生长的柱子。右侧是浏览器左上角原点，柱子像钟乳石一样从天花板倒挂下来。
> *   **Text**: 屏幕坐标系的投射

如果 AI 不理解倒置宇宙，生成的代码可能会导致图形方向反转。

#### 1.1.2 预留画框：防止裁切的保护区

AI 生成 D3 代码常犯的第二个错误，是忽略**边距惯例约束 (Margin Convention)**。
这其实是个**“画框留白”**协议。SVG（可缩放矢量图形）画布就像一张没有画框边沿的白纸，它默认没有任何内边距。如果直接贴着左上角 `(0,0)` 开始绘制 Y 轴，坐标轴外侧的刻度文字就会像被刀切掉一样超出版面，惨遭屏幕边缘切断。

> [VISUAL]
> *   **Slide**: S11b_Margin_Cutoff
> *   **Asset**: ![预览](../public/slides/s11b_margin_cutoff_placeholder.png)
> *   **Layout**: `Center`
> *   **Scene**: 一张完全没有留白边距的可视化图表，左侧和顶部的坐标系文字标签被边缘切断了。
> *   **Text**: Margin Convention 的边缘裁切预警

> [TECH NOTE] Margin Convention 机制
> Bostock 推行了 D3 Margin Convention：必须在画布外层预留 `top, right, bottom, left` 边距，并在内部创建 `<g>` 容器绘图。复查 AI 代码时，首要检查画布初始化是否遵守 Margin 惯例，避免坐标轴出界。"""

r2 = """### 1.1 空间倒置：浏览器坐标系法则完全违背直觉

#### 1.1.1 颠覆常识：向下坠落的倒置画布

> [VISUAL]
> *   **Slide**: S10b_Inverted_World
> *   **Asset**: ![预览](../public/slides/s10b_inverted_world_1779785545514.png)
> *   **Layout**: `Center`
> *   **Scene**: 以颠倒重力法则的视角俯视大地，建筑像钟乳石一样从天空向着地面下发疯狂生长。
> *   **Text**: 违背直觉的倒置画布

对于浏览器的渲染机制来说，其坐标系是一个**倒置画布 (Inverted Canvas)**：`(0,0)` 坐标原点默认定死在屏幕的**左上角**，Y 轴是**向下递增**的。这与我们自幼习惯的**左下角原点**向上生长的笛卡尔坐标系完全相反。也就是说，坐标数值从 0 变为 50，不是向上飞，而是意味着在屏幕上**向下坠落 50 像素**。这就是违背直觉的“倒置宇宙”。

> [VISUAL]
> *   **Slide**: S11_Inverted_Universe
> *   **Asset**: ![预览](../public/slides/s11_inverted_universe_1773994055292.png)
> *   **Layout**: `Split`
> *   **Scene**: 左侧是人类习惯的左下角原点向上生长的柱子。右侧是浏览器左上角原点，柱子像钟乳石一样从天花板倒挂下来。
> *   **Text**: 屏幕坐标系的投射

如果 AI 不理解倒置画布的机制，它生成的坐标映射会导致整个图形在屏幕上方向反转。

#### 1.1.2 预留画框：边距惯例的防裁切保护

AI 生成 D3 代码常犯的第二个错误，是忽略**边距惯例 (Margin Convention)**（即 D3 社区约定俗成的“画框留白”协议）。
SVG（可缩放矢量图形）画布就像一张没有边沿的纯白纸，默认没有任何内边距。如果直接贴着左上角 `(0,0)` 开始绘制 Y 轴，坐标轴外侧的刻度文字就会像被刀切掉一样，惨遭浏览器屏幕边缘切断。

> [VISUAL]
> *   **Slide**: S11b_Margin_Cutoff
> *   **Asset**: ![预览](../public/slides/s11b_margin_cutoff_placeholder.png)
> *   **Layout**: `Center`
> *   **Scene**: 一张完全没有留白边距的可视化图表，左侧和顶部的坐标系文字标签被边缘切断了。
> *   **Text**: Margin Convention 的边缘裁切预警

> [TECH NOTE] Margin Convention 机制
> D3 的作者推行了这一惯例：你可以把它想象成给装裱画作预留的“纸板外框”。代码层面上，必须用整体尺寸减去 `top, right, bottom, left` 的边距作为实际绘图区，并在内部创建一个平移过的 `<g>`（Group 图层组）容器来装载图表。复查 AI 代码时，首要检查这套“起手式”是否标准，以防坐标轴出界。

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 概念辨析：倒置坐标与边缘裁切
> *   **Q**: 独立开发者阿杰用 AI 生成了一个 D3 柱状图，但他发现图表的 Y 轴刻度文字被浏览器左侧切掉了一半，且所有柱子都像钟乳石一样从屏幕顶部往下生长。为了纠正这两个视觉错误，他应该重点检查和修正哪两项机制的实现？
> *   **Options**: A. 比例尺的值域 (Range) 映射方向，以及外层 SVG 的边距惯例 (Margin Convention) | B. 数据绑定的 Enter 态新增逻辑，以及比例尺的定义域 (Domain) 范围 | C. 箭头函数的取值探针写法，以及 Data Join 的 Exit 态元素销毁指令 | D. X 轴和 Y 轴的比例尺类型互换，以及对数比例尺 (scaleLog) 的使用
> *   **Answer**: `A`
> *   **Explain**: 正确答案是 A。柱子“像钟乳石一样向下生长”是因为浏览器原点在左上角（倒置宇宙），需要在 Y 轴比例尺的 `Range` 中反转映射（如从 `[0, height]` 改为 `[height, 0]`）。而文字被切掉则是未能遵守边距惯例（Margin Convention），没给坐标轴预留“画框留白”。选项 B、C、D 与本节讲述的空间坐标系和画布边缘安全区无关。"""

# Patch 3
t3 = """**比例尺 (Scales)** 是 D3 映射数据的核心构件。
你可以把它理解为**数据宇宙和像素世界之间的“汇率转换器”**。不管你的真实数据是几千亿的星辰还是零点几的尘埃，它都能帮你按比例**压缩或拉伸**，安全地安放到有限的屏幕内。代码中声明的比例尺包含两个**核心属性**："""

r3 = """**比例尺 (Scales)** 是 D3 映射数据的核心构件。
你可以把它理解为**数据和像素世界之间的“汇率转换器”**。它能帮你将真实数据按比例**压缩或拉伸**，安全地映射到有限的屏幕内。代码中声明的比例尺包含两个**核心属性**："""


# Patch 4
t4 = """面对指数级分布的数据，必须切换高阶比例尺：

> [VISUAL]
> *   **Slide**: S13_Scale_Types
> *   **Asset**: ![预览](../public/slides/s13_scale_types_1773994107704.png)
> *   **Layout**: `Split`
> *   **Scene**: 罗列出四种主要的比例尺类型及其用途。
> *   **List**: ["对数比例", "时间比例", "序数比例", "分段比例"]
> *   **Text**: 应对不同数据的比例尺

1.  **对数比例尺 (`scaleLog` / `scalePow`)**：专为极端跨度数据设计，使 10、100、1000 之间的视觉距离相等。
2.  **时间比例尺 (`scaleTime`)**：内置日历算法，将日期时间对象映射为像素点，避免大小月问题。
3.  **序数比例尺 (`scaleOrdinal`)**：用于离散类别（如省份名称）到颜色池（如十六进制色值）的映射分配。
4.  **分段比例尺 (`scaleBand`)**：用于计算柱状图中类别柱子的宽度及间隙。"""

r4 = """面对指数级分布的数据，默认比例尺会失效，我们必须切换**高阶比例尺**：

> [VISUAL]
> *   **Slide**: S13_Scale_Types
> *   **Asset**: ![预览](../public/slides/s13_scale_types_1773994107704.png)
> *   **Layout**: `Split`
> *   **Scene**: 罗列出四种主要的比例尺类型及其用途。
> *   **List**: ["对数比例", "时间比例", "序数比例", "分段比例"]
> *   **Text**: 应对不同数据的比例尺

1.  **对数比例尺 (`scaleLog` / `scalePow`)**：专治**极端悬殊数据**，强行把 10、100、1000 的视觉距离拉平，拯救被压缩的微小数值。
2.  **时间比例尺 (`scaleTime`)**：内置日历算法的**时间黑盒**，直接将日期对象映射为屏幕像素点，彻底避开大小月换算灾难。
3.  **序数比例尺 (`scaleOrdinal`)**：专做**离散分配**，例如把“广东、北京、上海”精准映射到特定的颜色池中。
4.  **分段比例尺 (`scaleBand`)**：柱状图的**专属分割器**，自动计算每根柱子的完美宽度与呼吸间隙。"""


# Patch 5
t5 = """> [VISUAL]
> *   **Slide**: S13b_Scale_Scroll
> *   **Asset**: ![预览](../public/slides/s13b_scale_scroll_1779785576595.png)
> *   **Layout**: `Center`
> *   **Scene**: 一个漂浮在深邃虚空中的魔法卷轴，上面流转着 scaleTime, scaleLog, scaleBand 等高阶函数的代码铭文。
> *   **Text**: D3 比例尺清单"""

r5 = """> [VISUAL]
> *   **Slide**: S13b_Scale_Scroll
> *   **Asset**: ![预览](../public/slides/s13b_scale_scroll_1779785576595.png)
> *   **Layout**: `Center`
> *   **Scene**: 一个漂浮在深邃虚空中的魔法卷轴，上面流转着 scaleTime, scaleLog, scaleBand 等高阶函数的代码铭文。
> *   **List**: ["scaleTime", "scaleLog", "scaleOrdinal"]
> *   **Text**: D3 比例尺清单"""

# Patch 6
t6 = """> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 场景判断：解救视觉塌陷
> *   **Q**: 实习生小王正在使用 D3 制作一个展示全球各国家碳排放量的条形图。他发现像中国和美国这样的排放巨头，其柱子长度直接冲破了屏幕边缘，而像图瓦卢这样的岛国，柱子只有 1 个像素的长度，完全看不见。为了让大小国家的相对差异都能在同一屏幕内被清晰辨认，他应该将决定柱子长度的比例尺替换为什么？
> *   **Options**: A. 线性比例尺 (scaleLinear) | B. 分段比例尺 (scaleBand) | C. 对数比例尺 (scaleLog) | D. 序数比例尺 (scaleOrdinal)
> *   **Answer**: `C`
> *   **Explain**: 选项 C 是正确答案。面对这种存在成百上千倍指数级落差的数据，`scaleLog` 可以将极端的数值跨度均匀化，使得 10 与 100 的视觉距离，等同于 100 与 1000 的视觉距离。若坚持使用默认的 `scaleLinear` (A)，小国的数据就会遭遇文中所述的“视觉塌陷”问题。选项 D 适用于离散类别映射，不适用于这种连续的数值比较。"""

r6 = """> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 场景判断：解救视觉塌陷
> *   **Q**: 实习生小王正在使用 D3 制作一个展示全球各国家碳排放量的条形图。他发现像中国和美国这样的排放巨头，其柱子长度直接冲破了屏幕边缘，而像图瓦卢这样的岛国，柱子只有 1 个像素的长度，完全看不见。为了让大小国家的相对差异都能在同一屏幕内被清晰辨认，他应该将决定柱子长度的比例尺替换为什么？
> *   **Options**: A. 线性比例尺 (scaleLinear) | B. 分段比例尺 (scaleBand) | C. 对数比例尺 (scaleLog) | D. 序数比例尺 (scaleOrdinal)
> *   **Answer**: `C`
> *   **Explain**: 选项 C 是正确答案。面对存在成百上千倍指数级落差的数据，`scaleLog` 可以将极端的跨度均匀化，使得 10 与 100 的距离等同于 100 与 1000 的视觉距离。若坚持使用默认的 `scaleLinear` (A)，小国数据就会遭遇本节前文所述的“贫富差距尺度的视觉塌陷”。选项 D (scaleOrdinal) 适用于离散类别的身份颜色映射，不适用于此类连续数值的大小比较。"""

# Patch 7
t7 = """**数据绑定机制 (Data Join)** 是 D3 的核心创新。
通俗地说，它就像是**给每个数据分配一个专属的“像素替身”**。传统手工编程需要你亲自去控制每一个替身的生老病死，容易出错且繁琐。而 D3 的 Data Join 让替身的生命周期完全交由背后的真实数据决定——数据来了替身就出现，数据变了替身就变形，数据没了替身就自动消亡。
#### 1.3.2 掌管轮回：灵魂与肉体的生灭状态

为处理传入的数据量与页面真实图形节点（DOM 元素）数量的不匹配，D3 设计了三态生命周期：

> [VISUAL]
> *   **Slide**: S14_Data_Join_Lifecycle
> *   **Asset**: ![预览](../public/slides/s14_data_join_lifecycle_1773994147382.png)
> *   **Layout**: `Split`
> *   **Scene**: 左侧显示数据的小球，中间是一条时间线，右侧是根据小球数量变化而自动生灭的柱形图。屏幕核心逐条罗列三大状态机制。
> *   **List**: ["Enter", "Update", "Exit"]
> *   **Text**: 数据驱动的三态轮回

在执行 `selectAll().data()` 后，这些数据灵魂会分化为三种**生命周期状态**：
1.  **Enter（进入态）**：**灵魂多于肉体**（数据多于 DOM 节点）。新数据找不到属于它的视觉替身，必须调用 `.append()` **降生**新节点。
2.  **Update（更新态）**：**灵魂附着肉体**（数据覆盖现有节点）。旧数据被新数值替换，需调用 `.transition().attr()` **变形**并更新现有节点的视觉属性。
3.  **Exit（离开态）**：**肉体失去灵魂**（DOM 节点多于数据）。旧数据已经离去，空壳替身必须调用 `.remove()` 被**销毁**，避免占据屏幕空间。

在使用大模型生成 D3 动画时，若发现旧图形不消失且新图形重叠，通常是因为 AI 遗漏了对 Exit 集合的 `remove()` 处理。我们通过一个诊断现场来巩固这个易错点。

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 错误诊断：幽灵数据的残留
> *   **Q**: 交互设计师小李让大模型生成一个 D3 动画，展示大屏上的活跃城市数。当数据从 5 个城市切换为只有 3 个城市时，他发现屏幕上依然立着 5 根柱子——前 3 根正确更新了，但最后面却诡异地残留了 2 根旧柱子。为了修复这个 bug，小李应该提示 AI 补充哪种数据绑定状态的处理逻辑？
> *   **Options**: A. 补充 Enter 态的元素新增指令 | B. 补充 Update 态的属性重写指令 | C. 补充 Exit 态的元素销毁指令
> *   **Answer**: `C`
> *   **Explain**: 答案是 C。当数据量从 5 减少到 3 时，“肉体（旧 DOM 节点）”多于“灵魂（新数据）”。那 2 根失去数据的旧柱子会进入 Exit（离开态），必须显式调用 `.remove()` 将其彻底物理销毁，否则就会像幽灵一样占据屏幕。选项 A 处理的是数据多于节点的情境，选项 B 对应的是前 3 根正确对接的柱子。

### 1.4 提取数值：使用箭头函数读取数据

> [VISUAL]
> *   **Slide**: S15_D3_Arrow_Function
> *   **Asset**: ![预览](../public/slides/s15_d3_arrow_function_1773994162619.png)
> *   **Layout**: `Center`
> *   **Scene**: 一群由代码组成的密密麻麻的小人，各自抱着属于自己的一个小黑板 `d`（代表 Data），小黑板上写着自己的薪水数值。
> *   **Text**: 数据探针 `d => ...`

在 D3 中设置属性时，通常使用箭头函数，如 `.attr('height', d => yScale(d.salary))`。
这里的 `d` (Datum) 代表绑定到当前节点的专属数据。当 D3 遍历图表元素时，`d =>` 会逐个提取数据的特定属性（如 `salary`）并传入比例尺，计算出具体的物理高度，从而实现对海量图形节点的批量渲染。

掌握了倒置画布、比例尺和 Data Join 三态循环后，你就能精准审查并修正 AI 生成的复杂 D3 代码。"""

r7 = """**数据绑定 (Data Join)** 是 D3 的核心创新（它解决了海量数据如何与页面视觉元素建立动态映射的难题）。
通俗地说，它就像是**为每一个数据点分配一个专属的“像素替身”**。传统的手工编程需要你亲自去控制每一个替身何时出现、何时消失，极易出错且繁琐。而 D3 的 Data Join 机制让替身的生命周期完全交由背后的真实数据全权接管——数据来了替身就出现，数据变了替身就变形，数据没了替身就自动消亡。

#### 1.3.2 掌管轮回：灵魂与肉体的生灭状态

为处理传入的数据量与页面真实图形节点（DOM 元素）数量的不匹配，D3 设计了三态生命周期：

> [VISUAL]
> *   **Slide**: S14_Data_Join_Lifecycle
> *   **Asset**: ![预览](../public/slides/s14_data_join_lifecycle_1773994147382.png)
> *   **Layout**: `Split`
> *   **Scene**: 左侧显示数据的小球，中间是一条时间线，右侧是根据小球数量变化而自动生灭的柱形图。屏幕核心逐条罗列三大状态机制。
> *   **List**: ["Enter", "Update", "Exit"]
> *   **Text**: 数据驱动的三态轮回

在执行 `selectAll().data()` 后，这些数据灵魂会分化为三种**生命周期状态**：
1.  **Enter（进入态）**：**灵魂多于肉体**（数据多于 DOM 节点）。新数据找不到属于它的视觉替身，必须调用 `.append()` **降生**新节点。
2.  **Update（更新态）**：**灵魂附着肉体**（数据覆盖现有节点）。旧数据被新数值替换，需调用 `.transition().attr()` **变形**并更新现有节点的视觉属性。
3.  **Exit（离开态）**：**肉体失去灵魂**（DOM 节点多于数据）。旧数据已经离去，空壳替身必须调用 `.remove()` 被**销毁**，避免占据屏幕空间。

在使用大模型生成 D3 动画时，若发现旧图形不消失且新图形重叠，通常是因为 AI 遗漏了对 Exit 集合的 `remove()` 处理。我们通过一个诊断现场来巩固这个易错点。

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 错误诊断：幽灵数据的残留
> *   **Q**: 交互设计师小李让大模型生成一个 D3 动画，展示大屏上的活跃城市数。当数据从 5 个城市切换为只有 3 个城市时，他发现屏幕上依然立着 5 根柱子——前 3 根正确更新了，但最后面却诡异地残留了 2 根旧柱子。为了修复这个 bug，小李应该提示 AI 补充哪种数据绑定状态的处理逻辑？
> *   **Options**: A. 补充 Enter 态的元素新增指令 | B. 补充 Update 态的属性重写指令 | C. 补充 Exit 态的元素销毁指令
> *   **Answer**: `C`
> *   **Explain**: 答案是 C。当数据量从 5 减少到 3 时，“肉体（旧 DOM 节点）”多于“灵魂（新数据）”。那 2 根失去数据的旧柱子会进入 Exit（离开态），必须显式调用 `.remove()` 将其销毁，否则就会像幽灵一样占据屏幕（回忆本节核心：数据驱动的三态轮回机制）。选项 A 处理的是“灵魂多于肉体”的降生情境，选项 B 对应的是前 3 根正确变形的柱子。

### 1.4 提取数值：使用箭头函数读取数据

> [VISUAL]
> *   **Slide**: S15_D3_Arrow_Function
> *   **Asset**: ![预览](../public/slides/s15_d3_arrow_function_1773994162619.png)
> *   **Layout**: `Center`
> *   **Scene**: 一群由代码组成的密密麻麻的小人，各自抱着属于自己的一个小黑板 `d`（代表 Data），小黑板上写着自己的薪水数值。
> *   **Text**: 数据探针 `d => ...`

在 D3 中给成千上万的替身设置属性时，我们极少写死具体数值，而是依赖**箭头函数探针**，比如 `.attr('height', d => yScale(d.salary))`。

这里的 **`d` (Datum)**，就像是挂在每个像素替身脖子上的**专属铭牌**。当 D3 的渲染引擎扫过整个图表时，`d =>` 探针会瞬间穿透每个数据点，提取出特定的值（例如 `salary`），再将其丢进比例尺中，计算出**绝对的物理像素高度**，最终在一瞬间完成对海海量数据的**批量渲染**。

只要掌握这三大核心机制：向下坠落的**倒置画布**、填平落差的**比例尺**，以及掌管元素的 **Data Join 三态轮回**，你就能精准拿捏并修正 AI 生成的任何 D3 代码。"""


replacements = [
    (t1, r1, "Patch 1"),
    (t2, r2, "Patch 2"),
    (t3, r3, "Patch 3"),
    (t4, r4, "Patch 4"),
    (t5, r5, "Patch 5"),
    (t6, r6, "Patch 6"),
    (t7, r7, "Patch 7")
]

for t, r, name in replacements:
    if t in content:
        content = content.replace(t, r)
        print(f"Applied {name}")
    else:
        print(f"Failed to find {name}!")

with open(file_path, "w", encoding="utf-8") as f:
    f.write(content)
print("Finished patching M03.")
