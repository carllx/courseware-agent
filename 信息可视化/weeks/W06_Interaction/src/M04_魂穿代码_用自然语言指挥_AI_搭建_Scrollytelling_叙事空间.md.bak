---
title: "魂穿代码 用自然语言指挥 AI 搭建 Scrollytelling 叙事空间"
---

## Module 4: 魂穿代码——用自然语言指挥 AI 搭建 Scrollytelling 叙事空间 (15 分钟)
<!-- BUDGET: 2700 chars | SLIDES: ≥3 | STATUS: done -->

完成了编剧与分镜工作，接下来就到了最具颠覆性的生产力执行环节：我们不是教你去手敲那复杂的 GSAP 和 D3.js 异步回调底层的框架文件库调用逻辑。

我们要利用上周所学过的 Vibe Coding（意感构建代码——即你只负责描绘意图和把控美感，把"敲砖头"的脏活累活全丢给 AI）和结构化提示词方法，将这套分镜直接压入大模型。请记住，你现在的身份不再是"背诵语法的代码复读机"，而是"手握图纸的最高指挥官"。

> [VISUAL]
> *   **Slide**: `S23_Prompt_AI_Architecture`
> *   **Layout**: `Code`
> *   **Scene**: 屏幕呈现的是一段结构犹如军事部署命令般的提示词架构，黑底绿字，每个层级缩进清晰，直指底层核心。在这段冰冷的代码架构上方，全息投影般悬浮着它刚刚生成的炫酷 Pinned 滚屏叙事页面效果作为光晕。
> *   **Text**: "跨越千行代码的鸿沟：全栈结构化 Prompt"
> *   **Asset**: ![预览](assets/slides/S23_Prompt_AI_Architecture.png)

大家仔细看屏幕上这层悬浮的光晕。这就是你们马上要通过自然语言亲手召唤出来的神迹。

这是一个非常工业级的模型结构。这根本不是给普通用户写周报用的那种简单的指令。一旦你们深刻理解了我们在 M2 拆解的叙事、触发、渲染这三个层级，任何现代大模型，无论是 GPT 还是 Claude，都会瞬间沦为你麾下最得力的、不用付工资的资深前端工程师军团。

**结构化 Prompt 的三层金字塔：三维元素的严格隔离**

在提问框内，你需要明确下发极具系统性的架构指令。请千万不要再使用诸如"帮我做一个好看的滚动数据图"这种外行且模糊的垃圾提示词。你要作为一名架构师，向机器发送**工程师级别**的军规限定。在这里，核心要诀是**隔离感（Isolation）**：你必须让 AI 清楚地意识到 HTML 骨架、CSS 黑场皮肤、GSAP 核心逻辑这三者是完全独立的子系统。如果你让 AI 一口全吞，它一定会给你生成一团面条一样的烂代码。

> [VISUAL]
> *   **Slide**: `S23b_Prompt_Pyramid`
> *   **Layout**: `Diagram`
> *   **Scene**: 一座清晰的三层金字塔。底层宽大，标注为"第一层：HTML 拓扑骨架"（声明 Pinned 布局、.step 容器数量、视口高度间距）；中间层标注为"第二层：库与生命周期绑定"（指定 ECharts + GSAP ScrollTrigger，声明触发点映射）；塔尖标注为"第三层：视觉调性与缓动情绪"（色盘约束、Ease 指令、响应式降级）。每层之间用箭头连接，标注"从骨骼到灵魂"。
> *   **Text**: "Prompt 金字塔：骨骼→神经→灵魂"
> *   **Asset**: ![预览](assets/slides/S23b_Prompt_Pyramid.png)

这座三层金字塔是你们把分镜表翻译成 AI 指令的唯一合法通道。

**第一层是 HTML 拓扑骨架（舞台承重墙）**。你必须在 Prompt 的开头就用最坚决的语气告诉模型先搭稳钢筋混凝土：页面采用经典的 Pinned 固定布局，左侧是占幅百分之四十的窄容器，装着若干个 `.step` 文本块，段落间距必须留出 80vh 高度的呼吸真空带；右侧占幅百分之六十，通过 CSS `position: sticky` 牢牢钉住在视口中心。如果你连这一层都不说清楚，AI 会自作主张地给你生成一个缺乏空间张力的普通单栏页面，所有你精心编排的滚动魔法将在起跑线上就宣告死亡。

**第二层是库与生命周期绑定（自动触发机关）**。也就是给舞台铺设暗藏的神经索。你需要明确且排他地指定技术栈：必须且仅使用 ECharts 用于数据渲染，GSAP 的 ScrollTrigger 插件用于滚动监听。接下来是最关键的一步——直接把你分镜表里的 X 轴触发文本和 Z 轴参数指令，逐条抄录进 Prompt。当第一个 `.step` 进入屏幕中线时触发什么函数，当第二个 `.step` 触碰辅助线时又切换成哪组数据。这一层的精度决定了舞台机关（代码逻辑）的开火命中率。

**第三层是视觉调性与缓动情绪（黑场皮肤与光影）**。你给 AI 下达的最后收尾指令，应当包含氛围约束（比如强烈的 CSS 黑场暗调设定，让图表成为页面中唯一的发光体）、色盘约束（全局最多三种主色）、缓动函数选型（比如 Step 2 的崩塌用 `power3.out`，Step 3 的回弹用 `back.out`）。这一层往往被新手忽略，但它恰恰是决定你的作品是"生硬的机房图表"还是"有灵魂的数据电影"的最终分水岭。

> [VISUAL]
> *   **Slide**: `S24_Attention_Decay`
> *   **Layout**: `Split`
> *   **Scene**: 左侧展示一张两千字的巨型提示词文本像瀑布般倾倒，但在漏斗底部严重堵塞，产出的代码齿轮支离破碎；右侧是折线图，展示“提示词长度与指令遵循率”的陡降曲线，在800词处标红断崖。
> *   **Text**: "大模型的注意力尾部衰减"
> *   **Source**: `AI_Gen`

[TEACHING MOMENT]
我见过太多同学在写 Prompt 时犯一个致命错误：他们一次性把全部需求倾倒在一段超过两千字的巨型指令里，试图让 AI 一口气吃成胖子。结果呢？大模型的注意力窗口被过多的并行约束撕扯稀碎，生成出来的代码往往在 HTML 结构上完美，但 ScrollTrigger 的触发逻辑却完全混乱。正确的做法是严格按照金字塔分层投喂：先用第一条 Prompt 命令搭骨架，确认布局无误后，再用第二条 Prompt 注入触发逻辑，最后用第三条补上视觉调性。每一轮对话只解决一层问题。这种迭代式 Prompt，远比一次性全量倾倒要高效十倍。

[DID YOU KNOW]
在 OpenAI 和 Anthropic 的内部基准测试中，当 Prompt 长度超过 800 个英文单词时，大模型对后半段指令的遵循准确率会产生明显的衰减。这被业界称为"注意力尾部衰减"。也就是说，你越想在一条 Prompt 里塞进更多细节，模型对最后出现的那些关键约束就越容易"视而不见"。这恰恰就是为什么我们要用金字塔分层法：把最关键的结构性约束放在每一条短 Prompt 的开头，让模型在注意力最集中的窗口期内完成最重要的架构决策。

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 三段式 Prompt 排障测试
> 
> Question: 小李正在使用大模型构建滚动交互网页。他发现页面刷新后，所有的 ECharts 图表瞬间就完成了进场动画，而此时读者根本还没滚动到对应的文字段落。如果要让 AI 修复这个问题，小李应该重点重写"Prompt 三层金字塔"中哪一层的指令？
> Options:
> A) 第一层：重新明确 HTML 拓扑骨架，要求加宽 `.step` 文本块的行间距
> B) 第二层：重新绑定生命周期，明确要求 ScrollTrigger 的触发点与 `.step` 的滚动位置严格映射
> C) 第三层：重新设定视觉调性，将 GSAP 的缓动函数（Ease）从 `power3.out` 改为线性匀速
> D) 第三层：增加 CSS 样式指令，强制给所有图表加上 `display: none` 属性
> Answer: B
> Explain: 图表动画过早触发，说明"动画播放（GSAP）"与"滚动行为（Scroll）"脱节，即没有正确设定触发机制。这属于第二层"库与生命周期绑定"（机关控制系统）的任务，比如未正确配置 ScrollTrigger 的 start/end 钩子。调整 HTML 间距（A）或表面缓动视觉（C/D）都无法触及底层的控制逻辑错误。

**AI 生成代码的二次调教：第一版永远只是草稿**

> [VISUAL]
> *   **Slide**: `S23c_AI_Iteration_Loop`
> *   **Layout**: `Diagram`
> *   **Scene**: 一个带有三次迭代箭头循环的流程图。第一圈标注"AI 首版输出 → 浏览器裸跑检视"；第二圈标注"发现问题 → 结构化 Prompt 修正"（旁注常见问题：sticky 失效、markers 残留、字体回退空白）；第三圈标注"手动微调间距 → 删除调试标记 → 完成品"。中央写着"不要指望一次搞定"。
> *   **Text**: "迭代三圈法则：AI 的草稿需要你的导演剪辑"
> *   **Asset**: ![预览](assets/slides/S23c_AI_Iteration_Loop.png)

请各位务必牢记这个铁律——AI 第一次生成的代码永远不是成品，它充其量只是一份粗糙的导演初剪。你必须经历至少两到三轮的精修迭代才能把它打磨成可以公开展示的状态。

第一圈是骨架验证。把代码复制到编辑器里，在浏览器中裸跑。此时你只关注一件事：页面的整体拓扑结构是否正确？左侧文字区是否真的在流动？右侧图表区是否真的被钉死了？如果发现 `position: sticky` 失灵，**不要试图自己去扒代码找 Bug**。指挥官不应亲自下场拧螺丝，你只需要截个图，或者把错误现象精确地向 AI 描述："右侧图表没有钉住视口，跟着文字滚走了，请检查外层容器的 overflow 设定或高度声明。"让 AI 作为你的排障员去修复它。

> [VISUAL]
> *   **Slide**: `S25_Markers_Debugging`
> *   **Layout**: `Full`
> *   **Scene**: 浏览器页面的实机运行截图。右侧边缘有非常醒目的红黄色辅助线（`scroller-start` 和 `start`），此时左侧的文本块刚好触碰辅助线，图表呈现触发动画的瞬间，辅助线旁边带有“导演场记板”的特效贴纸标注。
> *   **Text**: "开启 markers：交互导演的场记板"
> *   **Source**: `Manual`

第二圈是逻辑校准。你要逐个检查每一个 ScrollTrigger 触发点是否在正确的滚动位置开火。此时请务必在 Prompt 中要求 AI 开启 `markers: true` 属性——它是交互导演的"场记板和取景器"，会在屏幕上画出两条非常醒目的红黄色辅助线（如 `scroller-start` 和 `start`），精准标注触发的物理起点和终点。你可以清楚地看到，当你的文字块触碰这条辅助线的那一瞬间，右侧图表的机关是否同步如期开火。如果节奏错位了，立刻回到 Prompt 中向 AI 下令微调 `start` 和 `end` 参数配置。

> [VISUAL]
> *   **Slide**: `S26_Chinese_Typography`
> *   **Layout**: `Split`
> *   **Scene**: 中英文排版视觉负荷对比图：左侧是英文默认行高下的中文文本，字块像沉重的黑色砖墙般压迫视网膜（标红警示）；右侧是澎湃风格的中文排版，行高拉伸至 2.0，并拥有巨大的段落底部留白，呼吸感充足。
> *   **Text**: "中文环境下的排版与触发陷阱"
> *   **List**: ["错误示范", "澎湃风格"]
> *   **Source**: `Manual`

[CASE STUDY: 澎湃新闻风格的中文 Prompt 实战]
回到我们始终贯穿的本土语境。假设你的小组选题是"十年间中国城镇化率的断崖式跃迁"，数据来源于国家统计局。那么你的第一层拓扑 Prompt 除了声明 Pinned 架构之外，还必须额外叮嘱 AI 一个中国数字新闻界的排版隐规则：中文正文的 `font-family` 务必指定 `"Noto Sans SC", "PingFang SC", sans-serif` 作为回退链，`line-height` 强制拉高到 1.9 到 2.0。因为中文字符的方块结构比拉丁字母更加密集占位，如果使用英文默认行高，整个叙事文本块会像一堵沉重的黑色砖墙压向读者的视网膜，直接破坏你精心安排的呼吸节奏。这个细节，恰恰是澎湃新闻"美数课"团队在长期实践中反复打磨定型的中文可视化排版铁律。

此外，你们在第二层触发逻辑中还需要注意一个中文环境独有的排版陷阱：由于中文文本的自然段落长度通常比英文要短三到四成，这意味着同样内容量的 `.step` 文本块在中文页面中的物理高度会比英文小很多。如果你不对段落间距做额外放大处理，读者的滚轮会在还没来得及让大脑消化上一个数据转折的意义之前，就已经把下一段文字推到了触发线上。正确的做法是在中文场景下将每个 `.step` 的 `margin-bottom` 至少设为 `100vh`，甚至可以拉到 `120vh`，给予读者手指和大脑双重的缓冲窗口。

第三圈是情绪抛光。确认骨架和逻辑都没有问题之后，你要做的最后一件事就像电影的后期调色：向 AI 下达收尾指令，要求它"关闭 `markers` 调试标记，完全隐藏辅助网格线"。紧接着，基于你手指拨动滚轮的体验，去精确控制读者在两个事件节点间需要多大的心理缓冲空间。如果觉得节奏太赶，就继续向 AI 下达微调指令："将 `.step` 容器之间的等待留白（margin-bottom）增加 20vh"。这时候，你的身体感受就是检验代码节奏感的试金石。

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: markers 辅助线的场景应用
> 
> Question: 你的小组正在开发《中国乡镇经济演变》的 Scrollytelling 网页。在测试时，你们感觉"文字滚过一半时，右侧地图的省份颜色突然就变了"，节奏感很怪，但又说不清具体是哪里快了或慢了。此时向大模型发送什么指令能最有效、直观地排查这个节奏失调问题？
> Options:
> A) "请帮我重写整段页面的 HTML 代码，把文字段落拉长一倍。"
> B) "请在所有的 JS 函数中加入 `console.log`，打印每一毫秒的滚动坐标。"
> C) "请在 ScrollTrigger 的配置中开启 `markers: true` 属性，让页面显示可视化的红黄触发辅助线。"
> D) "请把图表颜色的变化速度调慢，从 0.5 秒延长到 2 秒。"
> Answer: C
> Explain: `markers: true` 是 GSAP ScrollTrigger 专门提供的可视化排障工具（即"导演控制板"），它能在页面上直接画出触发起点和终点的辅助线。开启它后，你能像看标尺一样，精确定位出图表颜色突变的具体物理位置，随后再让 AI 对症下药。盲目加长文字（A）或调慢动画（D）属于瞎猜盲调，而打印控制台日志（B）违背了 Vibe Coding 将技术细节交给 AI 的核心原则。

**(Pause: 2s)**

> [CASE STUDY]
> *   **Type**: `Practice`
> *   **Duration**: `80min`
> *   **Desc**: **"一人胜过整个前端交互重构组"——高燃落地实战。**
> 
> **全流程执行指令**：
> 1. 基于上个小节绘制的动效分镜表，提取出各自的文本段落与期望表现状态。
> 2. 套用上述"架构师三段式 Prompt"模板体系，打开你最顺手的现代大语言模型。要求 AI 一次性将 HTML 架构、强烈的黑场 CSS 布局设定、带有 GSAP ScrollTrigger 核心检测钩子绑架的 JavaScript 代码逻辑，统统生成合并为单一文件。
> 3. 将代码在浏览器中裸跑。
> 4. **调试修偏**：AI 第一次永远无法完美把控留白的距离和元素呼吸节奏。
>    - 将滚轮体验反馈给 AI（例如："滚动太快，请拉宽 .step 之间的等待留白"），指挥它修复呼吸节奏。
>    - 命令 AI 移除 markers 属性，关闭辅助网格线，隐藏导演控制板！
> 5. **展示高光时刻**：最后半小时，全班统一滑动滚轮鼠标——一幅幅带着厚重呼吸感、剧情跌宕起伏的数据重构名画，在屏幕上如交响乐般平滑演进。

这不仅仅是一堂设计课，你们此时此刻正在真正意义上蜕变升舱成为一名主宰观众视野与时间的**"空间编排导演"**。在这场宏大的数据风暴里，我们把最高的信息传递主动权，牢牢攥回到了手里。

---

