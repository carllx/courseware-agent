# M01：百分百标准化的力量——构建水平堆叠条形图

- `[VISUAL]`
  - Layout: `Center`
  - Title: 传统垂直柱状图的排版失效

> [WARNING]
> 回想一下你上一次让 AI 绘制“全国各省数据对比”时的灾难现场：X 轴的 30 多个省名糊成了一团黑色的墨迹；而且由于“广东”的总基数是“西藏”的近百倍，代表西藏的那根柱子直接被压缩成了贴在地板上的一条缝。你的“结构比例”分析，就这样被绝对基数的庞大阴影彻底吞噬了。

> [TEACHING MOMENT]
> 为了拯救这种“哑巴数据”，我们需要召唤**100% 标准化水平堆叠条形图（Stacked Normalized Horizontal Bar Chart）**。在这节课中，我们将通过意图驱动编程（Vibe Coding）范式，不再充当敲击 D3 语法的人肉打字机，而是作为可视化架构师，向 AI 递交一份明确的“空间与比例”重构契约。

## 确立归一化坐标：用水平布局重塑内部比例认知

- `[VISUAL]`
  - Layout: `List`
  - Title: 归一化水平布局的核心约束
  - List:
    - 计算架构
    - 认知一致
    - 降维补偿

展示不同州人口分布时，**水平布局**能解决几十个长文本州名的重叠问题。引入**100% 数据归一化**（堆叠扩展），能强制将视觉焦点从“绝对数量的比拼”转移到“内部成分比例分析”上。

但这引入了三个核心约束：
- **计算架构**：必须利用 D3 堆叠偏移器（相当于给每根柱子施加了“等长拉伸”魔法，无论原本基数多少，统统拉伸到相同长度），将数据拓展至 `[0, 1]` 空间（即 0% 到 100%），配合百分比坐标轴。
- **认知一致**：年龄段属于**有序数据（Ordinal）**——也就是像“幼、青、中、老”这样存在明确先后顺序、但无法进行常规加减的分类数据。这就要求我们在配色上必须顺应这种递进关系，防止 AI 随意分配五颜六色导致视觉逻辑混乱。
- **降维补偿**：归一化抹杀了绝对基数，交互层必须通过 Tooltip 补偿显示绝对数值，防止数据误导。

- `[VISUAL]`
  - Layout: `Center`
  - Title: Tooltip 降维补偿与连续色板展示

来，我们通过一个具体场景测试一下大家对这个“降维补偿”风险的理解。

> [ACTIVITY]
> Type: Quiz
> Duration: 2min
> Desc: 100% 归一化的误导风险
> Q: 小林正在用 D3.js 为公司开发各区域年度营收的图表。为了重点展示每个区域内“线上、线下、代理”三大渠道的结构比例，他直接渲染了一个 100% 标准化水平堆叠条形图，但没有配置交互提示框。在这种状态下交付，最容易让老板产生哪种决策误判？
> Options: A. 归一化拉伸抹杀了绝对基数，导致老板误以为总营收极低的区域和销冠区域的整体业绩规模完全一样 | B. 水平堆叠布局大幅度压缩了垂直坐标空间，导致老板在屏幕上无法看清图表左侧各个区域完整的文本标签 | C. 内部成分被转化为百分比并强制堆叠后，导致老板无法直观对比同一个区域内部各个渠道的具体占比差异 | D. 渠道类型被底层逻辑识别为有序数据并赋予连续渐变色，导致老板在视觉上难以清晰区分不同类型的渠道
> Answer: A
> Explain: 选择 A 正确。正如本节「降维补偿」所述，100% 归一化图表会强行将所有柱状图拉伸到同等长度，抹杀了各组原始体量的绝对差异。老板容易误判总体量，因此必须配置包含“原始绝对数值”的 Tooltip 进行补偿。选项 B 错误，水平布局的核心优势正是解决长文本重叠问题；选项 C 错误，比较内部占比正是 100% 堆叠图的设计初衷；选项 D 错误，渠道是无序分类数据，若误用渐变色属于 AI 编程错误，并非该图表类型固有的视觉误导机制。

## 签署底层架构契约：向 AI 下达确定性渲染指令

- `[VISUAL]`
  - Layout: `Text`
  - Title: Stacked Normalized Horizontal Bar Chart 架构契约

在意图驱动编程中，第一大忌就是“不给数据上下文直接下指令”。如果没有数据 Schema，AI 会产生严重的 D3 映射幻觉（比如瞎猜你的列名是 `value` 还是 `population`）。其次，如果不锁定环境，AI 极可能混入 Observable（一种专门面向数据科学的交互式 Web 笔记本平台）特有的 `FileAttachment` API。

因此，我们的 Prompt 必须是一份分两步走的**架构契约**：

- `[VISUAL]`
  - Layout: `Code`
  - Title: Step 1 - 数据上下文与底层约束
“你现在是一个资深数据可视化架构师。我需要在原生 HTML/JS 环境（非 Observable）下使用原生 d3.csv() 加载数据。
以下是我的数据结构 Schema 和样本（us-population-state-age.csv），请你先分析它的 Categorical/Quantitative 属性：

name,<10,10-19,20-29,30-39,40-49,50-59,60-69,70-79,≥80
AL,598478,638789,661666,603013,625599,673864,548376,316598,174781
AK,106741,99926,120674,102008,91539,104569,70473,28422,12503”

在 AI 成功识别数据后，我们再发送第二步的核心架构渲染指令。注意，我们要向 AI 传达“设计意图”，而不是充当它的 API 词典：

- `[VISUAL]`
  - Layout: `Code`
  - Title: Step 2 - 渲染意图与架构红线
“非常好。现在请使用 D3.js 构建一个 100% 标准化水平堆叠条形图 (Stacked Normalized Horizontal Bar Chart)。请严格遵循以下规范：
1. **数据归一化**：加载数据后，执行 100% 归一化堆叠计算。无论各州的原始总人口是多少，强制将每个州的各年龄段占比总和拉伸、拓展至 100%（即 `[0, 1]` 区间）。
2. **空间排版**：
   - Y 轴：映射州名，注意在每个水平条之间预留呼吸空间（Padding），防止图形上下粘连。
   - X 轴：作为连续比例尺，务必将域（Domain，即数据映射的输入范围上下限）严格锁定在 `[0, 1]`，并直接格式化为百分比显示。
3. **视觉映射红线**：年龄段属于**有序数据 (Ordinal)**，严禁使用彩虹色等无序离散色板！必须使用具备明度或色相连续渐变的连续色板（Sequential Scale），用颜色的深浅让视觉直观感受到年龄从幼至老的递进。
4. **交互补偿**：配置 Tooltip 时，必须双路输出。同时提供该组的『原始绝对数值』与计算后的『相对百分比』，以弥补归一化带来的绝对体量信息丢失。”

## 突破 AI 渲染幻觉：精准纠偏色彩序列与坐标基线

- `[VISUAL]`
  - Layout: `List`
  - Title: 视觉调试的三大纠偏方向
  - List:
    - 边界对齐
    - 色彩逻辑
    - 轴语义

拿到代码运行后，我们需要进入**视觉调试（Visual Debugging）**阶段，用精准指令纠正 AI 的“渲染幻觉”：

- `[VISUAL]`
  - Layout: `Grid`
  - Title: AI 渲染幻觉现场合集对比

1. **边界对齐**
   如果图表右侧像狗咬过一样参差不齐，说明 AI 忘记了归一化计算：
   *“图表右侧没有对齐到 100% 垂直基线。请检查 D3 的堆叠生成器是否启用了 100% 拓展偏移（Expand），并确认 X 轴的 Domain 已经严格锁定在 `[0, 1]`，不能有溢出。”*

2. **色彩逻辑**
   如果 AI 像发糖果一样给各年龄段分配了五颜六色：
   *“你使用了无序离散色板，违背了年龄段作为有序数据（Ordinal）的原则。请替换为具备明度渐变的连续色板（Sequential Scale），用颜色的深浅来体现年龄递增的物理逻辑。”*

3. **轴语义**
   如果 X 轴显示生硬的小数（如 0.1, 0.2）而非直观的比例：
   *“X 轴缺乏比例语义。请调用 D3 的格式化 API，将 X 轴数值全部格式化为带有 `%` 符号的百分比形式，符合人类阅读直觉。”*

为了检验大家是否掌握了这套视觉纠偏心法，我们来看一个真实的 AI 踩坑场景：

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 识别 AI 渲染幻觉并选择纠偏指令
> 
> Q: 实习生小王用 AI 编写一段 D3.js 代码，希望渲染出“全公司各部门人员年龄段分布”的 100% 水平堆叠条形图。代码运行后他发现一个严重 Bug：每个部门（即 Y 轴上的每一行）最右侧的柱子边界长短不一，有的部门因为人多拉得很长，人少的部门则很短。为了修复这个视觉 Bug，他应该向 AI 下达哪种纠偏指令？
> Options: A. “Y 轴上的部门名称挤在一起重叠了，请在每个水平条之间预留足够的呼吸空间（Padding）防止图形上下粘连” | B. “图表右侧没有对齐到 100% 垂直基线，请检查堆叠生成器是否启用了 100% 拓展偏移，并锁定 X 轴 Domain 为 [0, 1]” | C. “图表缺失了原始体量信息，请在配置交互提示框时同时输出原始绝对人数和计算后的百分比” | D. “你使用了无序的彩虹色板来代表年龄段，违背了物理逻辑，请立刻替换为带有明度渐变的连续色板”
> Answer: B
> Explain: 选择 B 正确。题干描述“右侧边界长短不一，受各部门总人数影响”，这表明图表目前只是普通的**绝对值堆叠条形图**，AI 忽略了核心的 100% 归一化计算步骤。因此需要向 AI 下令纠正堆叠生成器的拓展偏移与坐标轴范围。选项 A 用于修复标签拥挤；选项 C 用于弥补交互缺失；选项 D 用于修复色彩逻辑报错。这些指令都无法解决本题中“几何边界不对齐”的底层计算错误。
