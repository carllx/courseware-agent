## M02：驾驭非确定性空间——构建力导向关系网络

> [LIFE CONNECT]
> 当面对《悲惨世界》中几十个核心角色、上百条错综复杂的恩怨纠葛时，传统的柱状图或散点图完全失效了。面对动态且高密度的人际网络，我们需要一种机制——让数据自己去碰撞、排斥并寻找物理平衡点。

### 1.1 问题暴露：无约束的物理引擎将引发视觉灾难

> [WARNING]
> 当你让 AI 毫无顾忌地去渲染数百个人物关系时，屏幕上大概率会炸出一个“黑毛线团”（Hairball）。所有节点挤压在一起，粗壮的连线互相切割。为了驯服这种失控的图表，我们必须向 AI 颁布两条严格的物理戒律：

> [VISUAL]
> *   **Slide**: 驯服黑毛线团
> *   **Layout**: `Split`
> *   **Scene**: 复杂的“黑毛线团”杂乱关系图对比清晰的力导向网络图
> *   **List**: 线宽压缩 / 绑定心跳
> *   **Source**: `Code`

- **用非线性压缩对抗膨胀**：如果连线宽度随两人的共现频次（`value`）呈线性激增（比如相遇 100 次的线比 1 次的线粗 100 倍），画面会被粗线吞噬。必须要求 AI 对线宽施加 `Math.sqrt`（开平方根）压缩——也就是将巨大的数值落差压缩成温和的比例（把 100 和 1 的差距，压缩成 10 和 1 的差距）。
- **将坐标移交给“引擎心跳”**：力导向引擎像一个活着的物理世界，每一瞬间都在重新拉扯、推开节点。必须严禁 AI 像画静态图那样只画一次就固定坐标，而是要求它将所有的渲染画笔挂载到 `simulation.on("tick")` 上——就像游戏引擎里每一帧画面的刷新一样，这叫做力导向引擎的“心跳”。

### 1.2 实战引擎：精确的三重物理契约是驯服系统的关键

> [TECH NOTE]
> 在与 AI 结对编程时，对于力导向图这种复杂系统，必须精确定义三种力（连线引力、节点斥力、向心力）的物理契约。

> [VISUAL]
> *   **Slide**: 三重物理契约
> *   **Layout**: `Code`
> *   **Scene**: D3.js 力导向引擎 (forceLink, forceManyBody, forceCenter) 核心物理力代码片段展示
> *   **Source**: `Code`

请将以下指令分两步发送给 AI，第一步负责注入数据结构和分析目标，第二步下达带有架构红线的渲染指令：

> [VISUAL]
> *   **Slide**: 提示词 Step 1
> *   **Layout**: `Center`
> *   **Text**: Step 1 - 数据上下文与网络抽象
> *   **Scene**: 展示提供给 AI 的 JSON 数据上下文文本

“你现在是一个可视化架构师。我将在原生 HTML/JS 环境下使用 `d3.json()` 加载数据。
以下是我的网络数据结构 Schema 及样本（miserables.json），请先分析其节点属性与连接权重属性：

{
  "nodes": [
    {"name": "Myriel", "group": 1},
    {"name": "Napoleon", "group": 1}
  ],
  "links": [
    {"source": 1, "target": 0, "value": 1}
  ]
}”

在 AI 成功识别节点和链路模型后，再发出具体的渲染和引擎指令：

> [VISUAL]
> *   **Slide**: 提示词 Step 2.1
> *   **Layout**: `Center`
> *   **Text**: Step 2.1 - 物理引擎设定
> *   **Scene**: 展示提供给 AI 的物理引擎设定提示词段落

“很好，现在请用 D3.js (v7) 实现力导向图 (Force-Directed Graph)。请严格遵循以下架构：
1. **数据与物理引擎**：使用 `d3.json` 原生获取数据。激活三个基础力：`forceLink`（连线弹簧引力）、`forceManyBody`（节点互斥力）、`forceCenter`（向心重力）。
   - 【防崩塌关键】由于数据中的连线使用数组序号（0, 1...）作为索引，这完美契合 D3 的默认行为。因此，请直接使用 `d3.forceLink(links)`，绝对不要自作聪明地添加 `.id()` 身份映射器。
   - 画面每一帧的坐标刷新，必须绑定在 `simulation.on("tick")` 引擎心跳上。

> [VISUAL]
> *   **Slide**: 提示词 Step 2.2
> *   **Layout**: `Center`
> *   **Text**: Step 2.2 - 视觉映射与交互红线
> *   **Scene**: 展示提供给 AI 的视觉映射与交互干预提示词段落

2. **视觉映射**：
   - 节点颜色：识别 `group` 为分类数据，使用离散色板 `d3.schemeCategory10`。
   - 连线粗细：用 `Math.sqrt(d.value)` 将权重压缩为连线宽度，避免『黑毛线团』灾难；并设置透明度以便看透层叠。
3. **交互干预**：为节点添加拖拽行为 (`d3.drag`)。注意，力导向引擎默认会像陀螺一样慢慢停止计算（系统冷却），因此必须在拖拽时干预 `alpha` 衰减值，给系统“重新加热”，让网络在被拉扯时能再次灵动地弹跳起来。”

好，在进入下一步视觉诊断之前，我们先来做一个快速测验，看看大家对物理引擎边界的理解。

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 物理引擎边界判断
> *   **Q**: 数字媒体艺术系的小张用 AI 帮他编写一个《红楼梦》人物关系图的代码。运行后他发现，贾宝玉和林黛玉等节点像普通散点图一样死气沉沉地粘在固定的位置上，完全没有体现出复杂关系网互相推挤、拉扯的动态效果。他最可能是遗漏了哪一条指令？
> *   **Options**: A. 忘记将坐标渲染逻辑挂载到 `tick` 心跳循环上 | B. 忘记用 `Math.sqrt` 对共现频次进行非线性压缩 | C. 忘记在鼠标拖拽时通过 `alpha` 衰减值重新注入物理能量 | D. 忘记使用离散比例尺给不同家族分配颜色
> *   **Answer**: `A`
> *   **Explain**: 正确答案是 A。力导向引擎是一个活的物理世界，每一瞬间都在计算新的平衡位置。如果只画一次而不跟随 `tick`（心跳）逐帧刷新坐标，图表就会变成死气沉沉的静态图。选项 B 是用来防止线条过粗变成“黑毛线团”的；选项 C 是解决交互拖拽后网络不再回弹的问题，而不是初始状态不动的原因。

时间到！正确率还不错。接下来我们看看在图表动起来之后，会面临什么视觉灾难。

### 1.3 视觉诊断：视觉仲裁能有效夺回对图表的控制权

> [TEACHING MOMENT]
> AI 算得出物理方程式，但不懂视觉边界。当初始图表在屏幕上铺开时，必须立刻收回设计师的仲裁权，用这两把标尺进行审查与纠偏：

> [VISUAL]
> *   **Slide**: 视觉仲裁对比
> *   **Layout**: `Split`
> *   **Scene**: 错误渐变色和粗连线的图表对比应用了离散色板与底图层连线的正确图表
> *   **List**: 离散色板 / 连线置底
> *   **Source**: `Code`

1. **色彩阵营是否浑浊？（分类数据误用渐变）**
   如果不同帮派的人物颜色出现了渐变过渡，这是严重的编码映射错误。
   🗣 **对 AI 下达修正**：“人物的分组 `group` 是离散的分类数据。请用 `d3.scaleOrdinal`（序数比例尺，相当于专门用来给互不相干的类别发特定颜色的发牌器）配合离散分类色板（如 Tableau10），绝对不要使用连续型渐变色。”

2. **画面是否被喧宾夺主的连线淹没？（发丝团灾难重现）**
   如果高频次人物之间的连线过粗，甚至挡住了节点本身，必须立即实施视觉修正：
   🗣 **对 AI 下达修正**：“连线太夸张了，已经造成了视觉遮挡。请用 `Math.sqrt` 对线宽做非线性压缩。同时，必须把代表连线的 `<g>` 图层放到节点图层的下方（DOM 树先渲染连线），让节点全部浮出水面！”

> [ACTIVITY]
> *   **Type**: `Quiz`
> *   **Duration**: `2min`
> *   **Desc**: 视觉映射与图层控制判断
> *   **Q**: 实习生小陈在给漫威宇宙的力导向关系图上色时发现，分属“复仇者联盟”和“X战警”等不同帮派的节点颜色竟然呈现出模糊的渐变过渡状态，让人分不清阵营归属。为了修复这个编码映射错误，他应该向 AI 下达哪种修正指令？
> *   **Options**: A. 要求 AI 使用 `d3.scaleLinear` 将阵营属性映射到连续渐变色板上 | B. 要求 AI 使用 `d3.scaleOrdinal` 序数比例尺配合离散分类色板发牌 | C. 要求 AI 在 `simulation.on("tick")` 中重新计算每一帧的颜色数值 | D. 要求 AI 将节点图层移到连线图层的下方进行优先渲染
> *   **Answer**: `B`
> *   **Explain**: 正确答案是 B。参见本节对“色彩阵营浑浊”的诊断，阵营归属是互不相干的离散分类数据，必须使用序数比例尺（`scaleOrdinal`）搭配离散色板。选项 A 是引发该错误的根源（误用连续线性渐变）；选项 C 混淆了引擎的坐标计算与静态颜色映射；选项 D 解决的是节点被遮盖的图层顺序问题（且节点应放在连线上方、代码写在后面），与颜色无关。
