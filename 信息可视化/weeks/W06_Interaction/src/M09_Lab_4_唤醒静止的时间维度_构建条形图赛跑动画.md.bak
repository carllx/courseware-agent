## M04 唤醒静止的时间维度——构建条形图赛跑动画

> [VISUAL]
> *   **Slide**: 构建条形图赛跑动画
> *   **Layout**: `Full`
> *   **Scene**: 背景为动态上升和下降的红色与蓝色条形图，代表企业市值随年份变化的演变过程。

> [STORY TIME] 
> 回想一下你在 B 站或 YouTube 上刷到的那些几百万播放量的“全球科技公司市值 Top10 百年演变”视频。当看到诺基亚在 2007 年的巅峰后被苹果瞬间反超，那个急剧下降的红色条形图，是不是比任何静态的财报都令人印象深刻？

这就是**条形图赛跑（Bar Chart Race）**。它将枯燥的历史数据转化为动态的可视化序列。本节课我们将利用 AI 与 D3.js，构建条形图的动态交互动画。

### 锁定对象恒常性：数据身份绑定

> [VISUAL]
> *   **Slide**: 对象恒常性陷阱
> *   **Layout**: `Split`
> *   **Scene**: 对比图：左侧是没有绑定的错位条形图导致视觉错乱，右侧是绑定键值后平滑互换位置的正确演示。

做赛跑动画不能只依赖默认的过渡效果。如果不理解底层的**视觉心理学机制**，动画极易出现逻辑断层。

这场赛跑最大的技术陷阱，叫作**对象恒常性（Object Constancy）丢失**。
想象一下：2008 年代表“诺基亚”的柱子排在第一，2009 年它掉到了第二。如果系统只是粗暴地把第一根柱子变短、第二根柱子拉长，用户的眼睛就会错乱——“第一名怎么突然缩水了？”
我们必须向 AI 强调：通过 Key Function 键值绑定给数据赋予唯一身份标识。这就如同给每个包裹贴上独一无二的**“快递单号”**，无论包裹怎么在传送带上交换位置，系统都只认这一个单号。让视线能够追踪到同一实体柱子从高位平滑跌落。这不仅是为了防止画面闪烁，更是为了**维持数据追踪的连贯性**。

> [ACTIVITY]
> *   **Type**: Quiz
> *   **Context**: 某体育频道的转播大屏上，正在演示奥运奖牌榜的“条形图赛跑”动画。但当中国队的奖牌数超越美国队时，画面并没有表现出代表中国队的柱体“向上爬升并互换位置”的过程，而是原本在第一名的美国柱体瞬间变成了红色（中国），第二名瞬间变成了蓝色（美国）。
> *   **Question**: 造成这种视觉错乱的底层技术原因是什么？
> *   **Options**:
>     *   [A] 动画的刷新帧率过低，导致视觉上发生了卡顿
>     *   [B] 缺少了对象恒常性（Object Constancy）的键值绑定，系统没有追踪具体的国家实体
>     *   [C] 没有使用匀速摄像机（d3.easeLinear），导致位置互换的动画速度太快看不清
>     *   [D] 数据加载不完整，导致那一年的奖牌数据出现了断层
> *   **Answer**: B
> *   **Explain**: 当没有绑定唯一键值（如国家名）时，系统默认按排名索引更新数据。此时它只知道“把第一名的柱体变红并调整长度”，而不是“让中美的两根柱体交换物理位置”，这就破坏了对象恒常性（选 B）。这与帧率（A）或动画速度（C）无关，本质是 D3 丢失了对具体元素的身份追踪。

### 构建时序动画契约：向 AI 下达指令

> [VISUAL]
> *   **Slide**: D3 动画约束指令
> *   **Layout**: `Split`
> *   **Scene**: 左侧展示 AI 生成动画的核心参数配置，右侧高亮显示四个核心约束点。
> *   **List**: 原点对齐 / 颜色绑定 / 唯一键值 / 绝对匀速

我们必须在空间坐标、颜色映射和时间过渡三个层面，对 AI 设定严格的架构约束。为了降低理解底层的认知负担，我们可以把这些 D3.js 核心函数想象成三种日常工具：

- **`d3.scaleLinear`（等比卷尺）**：就像一把刻度均匀的卷尺，把成百上千亿的庞大数值按比例映射到屏幕上的确切像素长度。
- **`d3.schemeTableau10`（专属调色盘）**：就像画家手里固定的十色水彩盘，确保每一家企业从头到尾都分配到专属且不重复的颜色。
- **`d3.easeLinear`（匀速摄像机）**：就像一台没有任何加减速特效的匀速摄像机，确保时间流逝在每一帧都是绝对平均的，防止赛跑动画产生忽快忽慢的“粘滞感”。

理解了这三个工具，向你的 AI 工作台输入以下指令时，你就能清楚地知道每一句话在控制什么。

由于这涉及高维度的时间切片数据处理，为了防止 AI 在读取连续时间序列时搞错分类和数据类型，请向你的 AI 工作台分两步输入以下指令：

> [VISUAL]
> *   **Slide**: 提示词 Step 1
> *   **Layout**: `Code`
> *   **Text**: Step 1 - 时序追踪 Schema 输入
> *   **Scene**: 展示提供给 AI 的赛跑动画 Schema 样本数据

```text
你现在是一个动画数据可视化专家。我将在原生网页环境中用 d3.csv() 获取数据，不需要考虑 Observable 环境的特殊语法。
以下是用于条形图赛跑的 category-brands.csv 数据 Schema 和样本，请先确认各分类数据的绑定逻辑：

date,name,category,value
2000-01-01,Coca-Cola,Beverages,72537
2000-01-01,Microsoft,Technology,70196
```

确认完毕后，下发决定生死存亡的架构契约指令：

> [VISUAL]
> *   **Slide**: 提示词 Step 2
> *   **Layout**: `Code`
> *   **Text**: Step 2 - 赛跑动画架构契约
> *   **Scene**: 展示提供给 AI 的条形图赛跑架构约束指令

```text
收到。现在请使用 D3.js 构建一个流畅的条形图赛跑（Bar Chart Race）动画。
Architecture Constraints:
1. 数据环境：使用原生 d3.csv 获取数据，并确保 value 转换为数字类型，date 能正确按切片分组。
2. 坐标原点对齐：使用 d3.scaleLinear 映射 X 轴，Domain 必须强制从 0 开始。严禁自动截断最小值，避免比例失真。
3. 颜色分类绑定：根据 category 字段，使用纯粹的分类色板（如 d3.schemeTableau10）映射分类。分类颜色必须保持不变。
4. 数据唯一键值：在 .data() 绑定数据时，显式附加唯一键值 d => d.name，确保 DOM 元素在年份切换中维持物理追踪。
5. 匀速过渡动画：时间切片的过渡动画必须使用 .ease(d3.easeLinear) 维持帧间绝对匀速，拒绝非线性变速运动。
```

### 修复运动断层：截断与非匀速排查

> [VISUAL]
> *   **Slide**: 动态图表 Bug 排查
> *   **Layout**: `Center`
> *   **Scene**: 界面呈现针对动画截断、闪烁、非匀速的三个测试修复步骤。
> *   **List**: 截断测试 / 闪烁追踪 / 匀速排查

动态图表的 Bug 往往隐藏在帧与帧的过渡缝隙里。当图表渲染后，请运用以下策略排查逻辑漏洞：

1. **起跑线截断测试**
   如果发现 X 轴没有从 0 开始，导致长条的视觉长度比例严重失真，立刻纠偏：
   *“X 轴发生了截断。请修改 `xScale` 的 domain 强制设定为 `[0, d3.max(data)]`，将原点对齐。”*

2. **视盲与闪烁追踪测试**
   如果每次年份切换时，条形图发生闪烁或重绘，意味着对象恒常性丢失：
   *“动画出现节点闪烁。请检查 `.join` 前的数据绑定，确保 `.data()` 中正确传入了唯一键值函数 `(d) => d.name`。”*

3. **粘滞感与非匀速测试**
   如果长条在追赶时速度不均，视觉上会产生粘滞感：
   *“时间流逝必须是线性的。请在所有的 `.transition().duration()` 后附加 `.ease(d3.easeLinear)`，保证过渡处于匀速状态。”*
