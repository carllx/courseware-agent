# M03：时间序列的降维打击——构建日历网格热力图

本节课我们将面对高密度的数据挑战：展示 2019 年全年的电力使用数据。我们将摒弃传统的折线图，转而构建一个高密度的**日历时间网格热力图**。通过意图驱动编程，我们不仅是在写代码，更是在重构数据的时间维度。

### 派生多维时间矩阵：用颜色映射正负耗电波动

在将意图传递给 AI 之前，我们必须透彻理解这一图表的设计本质。

- **降维与派生**：我们要做的是把一维的连续时间序列“派生”成两个独立的维度——“每天（Day）”作为纵轴，“每小时（Hour）”作为横轴。这种矩阵化的降维能让周期性规律瞬间显现。
- **双向数据的色彩表达**：这是最大的难点所在。我们展示的是“净耗电量”，它具备正负双向特征。这要求我们必须使用**发散型色板（Diverging Colormap）**来精准表达。如果 AI 错误地混用了连续彩虹色板或线性标尺，将会导致网格错位与极其严重的视觉扭曲。

### 建立离散坐标与色彩映射：指挥 AI 绘制数据矩阵

我们需要用斩钉截铁的指令，锁死 AI 在坐标系生成和色彩分配上的发挥空间。

请复制并向 AI 发送这套严密的提示词契约：

> “请使用 D3.js 绘制一个电力消耗的日历网格热力图。遵循以下严格的架构与映射规范：
> **【数据与坐标系】**
> - **派生时间轴**：X 轴绑定 `date.getHours()`，Y 轴绑定 `d3.timeDay(d.date)`。
> - **标尺架构**：X/Y 轴**必须且只能使用 `d3.scaleBand()`**。绝对禁止使用 `d3.scaleLinear` 来计算网格坐标。
> **【视觉通道与颜色】**
> - 将“净耗电量”映射到 `rect` 的 `fill` 颜色。必须使用**发散型色板 (Diverging Colormap)**（例如 `d3.interpolateRdBu`），并确保中间值严格对齐零点。
> **【DOM 安全防线】**
> - 在 `<title>` 工具提示中，强制使用 `.text()` API 渲染内容，**严禁使用 `.html()`** 以防范安全风险。”

### 校准冷暖边界与网格阵列：修复视觉扭曲与错位

AI 在处理网格和发散型色彩时极易脱轨。一旦图表渲染出来，请用你的双眼进行严酷的审查：

1. **冷暖色彩边界是否精准？（零点漂移测试）**
   如果数据的正负零界线对应的颜色不是中性色（如白色或极浅的灰色），说明色系偏了：
   *“请立即改用以 0 为中心点的发散型标尺（Diverging Scale），不要使用普通的线性颜色过渡。”*

2. **矩阵网格分布是否均匀？**
   如果发现长方形网格大小不一，或者出现奇怪的重叠：
   *“你误用了线性标尺或高度映射。请复原为 `d3.scaleBand`，并把 `.bandwidth()` 的值直接赋给 rect 的 width 和 height 属性。”*
