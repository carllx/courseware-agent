# M01：百分百标准化的力量——构建水平堆叠条形图

> [VISUAL]
> Layout: Image
> Title: 传统垂直柱状图的排版失效

> [LIFE CONNECT]
> 假设你拿到美国 50 个州各年龄段的人口分布数据，如果直接让 AI 绘制普通垂直柱状图，会遇到两个问题：第一，50 个州的 X 轴文本标签会互相重叠；第二，由于加州总人口是怀俄明州的近 70 倍，后者的图形会被绝对基数压制到几乎不可见。
> 
> [TEACHING MOMENT]
> 在这节课中，我们将通过意图驱动编程（Vibe Coding）范式，指挥 AI 运用**水平堆叠条形图（Stacked Normalized Horizontal Bar Chart）**重塑数据呈现。我们的核心任务是将专业的设计意图，转化为明确的架构契约，而非逐行编写 D3 基础语法。

## 确立归一化坐标：用水平布局重塑内部比例认知

> [VISUAL]
> Layout: List
> Title: 归一化水平布局的核心约束
> List:
>   - 计算架构
>   - 认知一致
>   - 降维补偿

展示不同州人口分布时，**水平布局**能解决几十个长文本州名的重叠问题。引入**100% 数据归一化**（堆叠扩展），能强制将视觉焦点从“绝对数量的比拼”转移到“内部成分比例分析”上。

但这引入了三个核心约束：
- **计算架构**：必须利用 D3 堆叠偏移器（相当于给每根柱子施加了“等长拉伸”魔法，无论原本基数多少，统统拉伸到相同长度），将数据拓展至 `[0, 1]` 空间（即 0% 到 100%），配合百分比坐标轴。
- **认知一致**：年龄段属于**有序数据（Ordinal）**——也就是像“幼、青、中、老”这样存在明确先后顺序、但无法进行常规加减的分类数据。这就要求我们在配色上必须顺应这种递进关系，防止 AI 随意分配五颜六色导致视觉逻辑混乱。
- **降维补偿**：归一化抹杀了绝对基数，交互层必须通过 Tooltip 补偿显示绝对数值，防止数据误导。

来，我们通过一个具体场景测试一下大家对这个“降维补偿”风险的理解。

> [ACTIVITY]
> Type: Quiz
> Duration: 2min
> Desc: 100% 归一化的误导风险
> Q: 小林正在用 D3.js 为公司开发各区域年度营收的图表。为了重点展示每个区域内“线上、线下、代理”三大渠道的结构比例，他直接渲染了一个 100% 标准化水平堆叠条形图，但没有配置交互提示框。在这种状态下交付，最容易让老板产生哪种决策误判？
> Options: A. 归一化拉伸抹杀了绝对基数，导致老板误以为总营收极低的区域和销冠区域的整体业绩规模完全一样 | B. 水平堆叠布局大幅度压缩了垂直坐标空间，导致老板在屏幕上无法看清图表左侧各个区域完整的文本标签 | C. 内部成分被转化为百分比并强制堆叠后，导致老板无法直观对比同一个区域内部各个渠道的具体占比差异 | D. 渠道类型被底层逻辑识别为有序数据并赋予连续渐变色，导致老板在视觉上难以清晰区分不同类型的渠道
> Answer: A
> Explain: 选择 A 正确。正如本节「降维补偿」所述，100% 归一化图表会强行将所有柱状图拉伸到同等长度，抹杀了各组原始体量的绝对差异。老板容易误判总体量，因此必须配置包含“原始绝对数值”的 Tooltip 进行补偿。选项 B 错误，水平布局的核心优势正是解决长文本重叠问题；选项 C 错误，比较内部占比正是 100% 堆叠图的设计初衷；选项 D 错误，渠道是无序分类数据，若误用渐变色属于 AI 编程错误，并非该图表类型固有的视觉误导机制。

## 签署底层架构契约：向 AI 下达确定性渲染指令

> [VISUAL]
> Layout: Code
> Title: Stacked Normalized Horizontal Bar Chart 架构契约

在意图驱动编程中，第一大忌就是“不给数据上下文直接下指令”。如果没有数据 Schema，AI 会产生严重的 D3 映射幻觉（比如瞎猜你的列名是 `value` 还是 `population`）。其次，如果不锁定环境，AI 极可能混入 Observable 特有的 `FileAttachment` API。

因此，我们的 Prompt 必须是一份分两步走的**架构契约**：

> [VISUAL]
> Layout: Code
> Title: Step 1 - 数据上下文与底层约束
> 
> “你现在是一个资深数据可视化架构师。我需要在原生 HTML/JS 环境（非 Observable）下使用原生 d3.csv() 加载数据。
> 以下是我的数据结构 Schema 和样本（us-population-state-age.csv），请你先分析它的 Categorical/Quantitative 属性：
> 
> name,<10,10-19,20-29,30-39,40-49,50-59,60-69,70-79,≥80
> AL,598478,638789,661666,603013,625599,673864,548376,316598,174781
> AK,106741,99926,120674,102008,91539,104569,70473,28422,12503”

在 AI 成功识别数据后，我们再发送第二步的核心架构渲染指令，包含精确的技术护栏：

> [VISUAL]
> Layout: Code
> Title: Step 2 - 渲染指令与架构红线
> 
> “非常好。现在请使用 D3.js 构建一个 100% 标准化水平堆叠条形图 (Stacked Normalized Horizontal Bar Chart)。请严格遵循以下规范：
> 1. **数据处理**：使用 `d3.csv` 并在回调中做好字符串到数字的类型转换。使用 `d3.stack().offset(d3.stackOffsetExpand)` 对数据进行归一化堆叠计算，派生出 `[0, 1]` 区间的比例数据。
> 2. **空间与比例尺**：
>    - Y 轴：使用 `d3.scaleBand` 映射州名 (States)，并配置 `paddingInner` 创建负空间隔离，防止视觉粘连。
>    - X 轴：使用 `d3.scaleLinear`，强制域为 `[0, 1]`，并通过 `.tickFormat()` 渲染为百分比格式。
> 3. **视觉映射红线**：映射年龄段的颜色通道时，识别其为**有序数据 (Ordinal)**。严禁使用 `schemeCategory10` 等无序离散色板，必须使用具备明度或色相连续渐变的连续色板，以呈现年龄从幼至老的自然递进。
> 4. **交互设计**：配置 Tooltip 时，必须双路输出。同时提供该组的『原始绝对数值』与计算后的『相对百分比』，以弥补归一化带来的绝对体量信息丢失。”

## 突破 AI 渲染幻觉：精准纠偏色彩序列与坐标基线

> [VISUAL]
> Layout: List
> Title: 视觉调试的三大纠偏方向
> List:
>   - 边界对齐
>   - 色彩逻辑
>   - 轴语义

拿到代码运行后，我们需要进入**视觉调试（Visual Debugging）**阶段，用精准指令纠正 AI 的“渲染幻觉”：

1. **边界对齐**
   如果图表右侧未对齐 100% 基线，请下达纠偏指令：
   *“图表右侧没有对齐到 100% 基线。请检查 stack generator 是否正确应用了 `d3.stackOffsetExpand` 偏移器，并确认 X 轴的 Domain 是否被严格锁定在 `[0, 1]`。”*

2. **色彩逻辑**
   如果 AI 像彩虹糖一样给各年龄段分配了毫无关联的颜色，需指出其错误：
   *“你使用了无序离散色板，违背了年龄段作为有序数据（Ordinal，即存在自然先后顺序的数据）的原则。请替换为 Sequential Scale（连续比例尺，如随着数值变大颜色逐渐变深的连续渐变色板），以体现年龄递增的逻辑。”*

3. **轴语义**
   如果 X 轴显示小数（如 0.1, 0.2）而非百分比：
   *“X 轴缺乏比例语义。请在 X 轴生成器上应用 `.tickFormat(10, '%')` 将数值格式化为百分比。”*
