# M04：唤醒静止的时间维度——构建条形图赛跑动画

在最后一节课，我们要让数据真正“动”起来。我们将利用 D3.js 构建一个极具叙事张力的**条形图赛跑（Bar Chart Race）**，展示随时间演变的娱乐消费数据。在这里，你将深刻体会到“眼睛胜过记忆”的设计法则。

### 锁定对象恒常性：用平滑插值驱动赛跑叙事

做动态图表不仅仅是加几个动画 API 那么简单。我们需要向 AI 强调动画背后的视觉心理学机制：

动画的核心是**对象恒常性（Object Constancy）**。当一个长条从第五名跃升到第二名时，用户的眼睛必须能持续追踪这个具体对象，而不是看到图表闪烁后直接呈现最终结果。只有建立严谨的键值绑定（Key Function）和**平滑插值（Data Interpolation）**，才能让动画不显得抽搐，这也是决定当前项目成败的底层架构。

### 构建时序动画契约：向 AI 明确关键帧与插值逻辑

我们要通过提示词，在空间映射、色彩身份和时间补间三个层面上给 AI 设定死规定。

向你的 AI 工作台输入以下指令：

> “Task: 请使用 D3.js 帮我构建一个流畅的‘条形图赛跑（Bar Chart Race）’动画。
> Architecture Constraints:
> 1. 比例尺：使用 `d3.scaleLinear` 映射 `value` 到 X 轴，Domain 必须严格从 `0` 开始。
> 2. 颜色映射：使用纯粹的分类身份色板（如 `d3.schemeTableau10`）映射 `category` 属性。
> 3. 对象恒常性：在 `.data()` 中显式提供唯一键值函数 `d => d.name`。
> 4. 补间与平滑机制：通过 `d3.pairs` 处理时间切片，生成关键帧。过渡动画必须使用 `.ease(d3.easeLinear)` 维持帧间绝对匀速。”

### 追踪帧间闪烁与运动断层：修复动画截断与非匀速

动态图表的 Bug 往往隐藏在帧与帧的过渡之间。让动画跑起来，运用以下策略捕获 AI 的错误：

1. **零点截断测试**
   如果发现 X 轴没有从 0 开始，导致长条的长度比例严重失真（例如 100 和 110 看上去差了一倍），立刻纠偏：
   *“X 轴发生了截断。请修改 `xScale` 的 domain 强制设定为 `[0, d3.max(data)]`。”*

2. **视盲与闪烁追踪测试**
   如果每次年份切换时，条形图都会闪烁或者重新绘制，这意味着对象恒常性丢失了：
   *“动画出现节点闪烁。请检查 `d3.join` 前的数据绑定，确保 `.data()` 中传入了 key function `(d) => d.name`。”*

3. **匀速运动测试**
   如果条形图在变长或位移时忽快忽慢，给人的视觉感受会非常粘滞：
   *“在所有的 `.transition().duration(time)` 后增加 `.ease(d3.easeLinear)` 以保证动画始终处于绝对匀速状态。”*
