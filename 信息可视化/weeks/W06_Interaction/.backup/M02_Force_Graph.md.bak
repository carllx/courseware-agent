# M02：驾驭非确定性空间——构建力导向关系网络

在掌握了标准坐标系后，我们将步入一个非确定性的视觉空间。这节课，我们要基于 D3.js 实现一个《悲惨世界》人物共现关系的**节点-链接图（Node-Link Diagram）**。我们将运用意图驱动编程的方法，让 AI 替我们处理复杂的物理引擎和节点结算。

### 锁定引擎物理边界：用规则防范网络感知灾难

力导向图的魅力在于它的动态与有机，但这同时也是它的致命弱点。为了避免图表沦为一团乱麻，我们必须提前向 AI 明确核心难点：

- **感知灾难防范**：在映射网络关系时，最常犯的错误有两个：一是将连续色板误用于分类数据（例如人物所属的群体 `group`）；二是让连线宽度随权重（`value`）线性激增，导致满屏粗线，引发可怕的“发丝团效应（Hairball）”和严重的视觉遮挡。
- **物理引擎边界隔离**：D3 的力导向引擎会接管元素的坐标空间，这个空间是非确定性的。因此，必须强制要求 AI 将所有渲染更新严格隔离在 `simulation.on("tick")` 的回调循环中，绝对禁止以静态方式强行干预坐标。

### 注入核心物理法则：构建节点与连线的渲染契约

对于这种包含多个力学模型的复杂图表，我们需要给 AI 提供一份异常精确的架构清单。

请将以下提示词直接发送给你的 AI 助手：

> “请用 D3.js (v7) 帮我实现一个《悲惨世界》人物关系的力导向图 (Force-Directed Graph)。请严格遵循以下可视化编码与架构约束：
> 1. **物理引擎架构 (Simulation Setup)**：
>    - 初始化 `d3.forceSimulation`，包含力模型：`forceLink` (连线力), `forceManyBody` (节点斥力), `forceCenter` (向心力)。
>    - 【关键】定义连线力时，必须显式声明节点主键映射：`d3.forceLink(links).id(d => d.id)`，绝对禁止使用默认的数组索引绑定。
>    - 所有视图（节点与连线）的坐标更新逻辑，必须严格封装在 `simulation.on("tick", callback)` 引擎滴答回调内。
> 2. **视觉编码映射 (Visual Encodings)**：
>    - 节点 (Nodes)：身份通道映射分类数据 `group`。必须使用离散分类色板 `d3.schemeCategory10`。
>    - 连线 (Links)：量值通道映射共现频次 `value` 到连线宽度。必须使用 `Math.sqrt(d.value)` 对线宽映射进行视觉非线性压缩。统一设置 `stroke-opacity: 0.6`。
> 3. **交互行为 (Interactions)**：为节点挂载拖拽行为 (d3.drag)，并在拖拽生命周期中干预 alpha 衰减值。”

### 制裁视觉遮挡与异常映射：修复 AI 的连线失控

力导向图往往需要经过多轮的参数调优。当你看到初版渲染结果时，立刻启动以下视觉审查流程：

1. **社群颜色是否出现渐变或混淆？**
   如果人物的分组颜色模糊不清，立即纠正：
   *“请检查 `group` 字段的颜色映射比例尺，强制替换为 `d3.scaleOrdinal(d3.schemeTableau10)`，不要使用连续型映射。”*

2. **线条是否过于粗壮或喧宾夺主？**
   如果高频次人物之间的连线粗得像树干，甚至挡住了节点，直接下达制裁指令：
   *“当前连线的线宽太夸张。请加上 `Math.sqrt` 压缩极端值，并确保连线 `<g>` 标签位于节点 `<g>` 标签的下方（即在 DOM 树中更早渲染）。”*
