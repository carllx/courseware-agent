# RFC: 教务生成引擎升级需求书 (Admin Engine Upgrade)

## 背景 (Context)
随着《2025-2026-2 课程》项目完成底层 SSOT (单一事实来源) 数据源的拆解重构，课程端的实验与考核元数据已独立剥离至 `course_experiments.yaml` 与 `course_assessment.yaml` 中。
同时，教务处下发了全新的 4 条教务管理规范。经【架构边界分析】，涉及“排版表现”与“物流装袋”的规范需由教务支撑引擎 (`nfu - 教务`) 承接。

本 RFC 正式将这部分视图渲染与后处理需求移交至教务引擎开发组。

---

## 需求项 1：电子版与纸质版档案物流打包 (新增 pipeline)

**所属模块**：`scripts/` (建议新增 `pack_archive.py`)
**业务需求**：
教务处要求期末实验档案必须同时满足“纸质材料袋”与“电子U盘归档”双轨要求。由于课程项目仅负责输出原始产物，教务引擎需提供最后一公里的打包物流能力：
1. **电子归档文件重命名机制**：能够根据学生花名册，将学生的电子版作业/报告批量规范化重命名为：`[学号]+[姓名]+[实验/作业名称].ext`。
2. **纸质版装袋核对单生成**：自动生成一份 Excel 或 Markdown 格式的核对清单（含：大纲、认定表、实验指导书、评分表等字段），供辅导员或教师勾选装袋。
3. **抽样机制预留**：预留接口，支持根据输入比例（如 10%）自动随机抽样“大中小”三个分数段的实验报告用于专项打印审查。

---

## 需求项 2：统一教案模板与排版元素渲染 (更新渲染引擎)

**所属模块**：`docx_engine.py` 及底层模板占位符 (Word/HTML Templates)
**业务需求**：
针对“统一使用学院模板”、“教师留白”、“理论课备注”等排版硬性约束，教务引擎在套用数据源时必须实现以下映射表现：
1. **统一模板强制换装**：必须使用指定的学院标准 Docx 模板套版，严禁使用课程侧传入的自定义样式。
2. **实验报告评分区域留白**：在生成的单篇实验报告/大纲末尾，必须渲染一块固定的【教师评语与评分】区域（无需填充数据，必须保留视觉留白）。
3. **理论与实践类型的视觉标识**：
   - 引擎在读取 `course_calendar.yaml` 时，若发现某一周次的类型为纯“理论课”，需在生成的教案表头醒目处（或备注栏）自动打上【本周为纯理论授课，无上机操作】的免责备注说明。
   - 实验大纲与日历进度表的标题必须绝对对应，引擎在生成进度表时应直接抓取大纲的 Title 进行绑定渲染。

---

**交付验收条件**：
- 课程侧的数据流（`course_loader.py`）无需因为这些排版与物流规则增加任何冗余字段。
- 能够通过单命令流（如 `python pack_archive.py --target=viz_course`）一键输出符合新规的归档压缩包。
